% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/converters.R
\name{tbl_now_to_EpiNow2}
\alias{tbl_now_to_EpiNow2}
\title{Convert a \code{tbl_now} into \pkg{EpiNow2} input}
\usage{
tbl_now_to_EpiNow2(x, ..., verbose = TRUE)
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{...}{Forwarded to \code{\link[data.table:as.data.table]{data.table::as.data.table()}}.}

\item{verbose}{Logical. Print the choices that were made.}
}
\value{
A \code{data.table} with columns \code{date} and \code{confirm}.
}
\description{
\pkg{EpiNow2} works with a single incidence time series (\code{date}, \code{confirm})
and therefore has no delay/report dimension. This function collapses a
\code{tbl_now} to that single time series, keyed on the \code{event_date}, using the
most recently reported counts (see \code{\link[=get_latest_reported_cases]{get_latest_reported_cases()}}).

Because \pkg{EpiNow2} has only one time index, there is intentionally \strong{no}
\code{tbl_now_from_EpiNow2()}.
}
\details{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\examples{
\dontshow{if (requireNamespace("data.table", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(mpoxdat)
nowobj <- tbl_now(mpoxdat, event_date = "dx_date", report_date = "dx_report_date",
                  case_count = "n", data_type = "count-incidence", verbose = FALSE)
tbl_now_to_EpiNow2(nowobj, verbose = FALSE)
\dontshow{\}) # examplesIf}
}
