% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/converters.R
\name{tbl_now_to_EpiNow2}
\alias{tbl_now_to_EpiNow2}
\title{Convert a \code{tbl_now} into \pkg{EpiNow2} input}
\usage{
tbl_now_to_EpiNow2(
  x,
  ...,
  model = c("estimate_infections", "estimate_truncation"),
  verbose = TRUE
)
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{...}{Forwarded to \code{\link[data.table:as.data.table]{data.table::as.data.table()}}.}

\item{model}{Which \pkg{EpiNow2} model the output targets:
\code{"estimate_infections"} (default; a single \code{date}/\code{confirm} series for
\code{\link[EpiNow2:estimate_infections]{EpiNow2::estimate_infections()}} / \code{\link[EpiNow2:epinow]{EpiNow2::epinow()}}) or
\code{"estimate_truncation"} (a list of \code{date}/\code{confirm} snapshots for
\code{\link[EpiNow2:estimate_truncation]{EpiNow2::estimate_truncation()}}).}

\item{verbose}{Logical. Print the choices that were made.}
}
\value{
For \code{"estimate_infections"}, a \code{data.table} with columns \code{date} and
\code{confirm}. For \code{"estimate_truncation"}, a list of such \code{data.table}s
ordered from earliest to latest report snapshot.
}
\description{
\pkg{EpiNow2}'s renewal-equation models (\code{\link[EpiNow2:estimate_infections]{EpiNow2::estimate_infections()}},
\code{\link[EpiNow2:epinow]{EpiNow2::epinow()}}) take a \strong{single incidence time series} (\code{date},
\code{confirm}) with no report/delay dimension. For these
(\code{model = "estimate_infections"}, the default) the \code{tbl_now} is collapsed to
that single series using the most recently reported counts per \code{event_date}
(equivalent to \code{\link[=get_latest_reported_cases]{get_latest_reported_cases()}}).

\code{\link[EpiNow2:estimate_truncation]{EpiNow2::estimate_truncation()}} is different: it takes \strong{multiple
snapshots} of the same series observed at successive report dates and \emph{does}
use the report dimension. With \code{model = "estimate_truncation"} the \code{tbl_now}
is turned into that list of \code{date}/\code{confirm} snapshots (one per report date,
earliest first) — precisely the information the report dimension encodes.

There is intentionally \strong{no} \code{tbl_now_from_EpiNow2()}: neither a single time
series nor a set of snapshots can, in general, reconstruct the full
event/report structure of a \code{tbl_now}.
}
\details{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\examples{
\dontshow{if (requireNamespace("data.table", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(mpoxdat)
nowobj <- tbl_now(mpoxdat,
  event_date = "dx_date", report_date = "dx_report_date",
  case_count = "n", strata = "race",
  data_type = "count-incidence", verbose = FALSE
)
# single series for estimate_infections()
tbl_now_to_EpiNow2(nowobj, verbose = FALSE)

# report-date snapshots for estimate_truncation()
tbl_now_to_EpiNow2(nowobj, model = "estimate_truncation", verbose = FALSE)
\dontshow{\}) # examplesIf}
}
