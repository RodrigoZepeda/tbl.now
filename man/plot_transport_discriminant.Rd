% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostic_plot.R
\name{plot_transport_discriminant}
\alias{plot_transport_discriminant}
\title{Plot the transport-discriminant plane}
\usage{
plot_transport_discriminant(
  x,
  ...,
  plotly = FALSE,
  size = 1,
  grid_linewidth = 0.3,
  palette = .tbl_now_palette()
)
}
\arguments{
\item{x}{A \code{\link[=tbl_now]{tbl_now()}} object.}

\item{...}{Passed to \code{\link[=transport_discriminant]{transport_discriminant()}} (e.g. \code{lookback}, \code{period},
\code{alpha}).}

\item{plotly}{If \code{TRUE}, return an interactive \pkg{plotly} widget instead of a
static plot. Default \code{FALSE}.}

\item{size}{Multiplier on the size of the points and their date labels.
Default \code{1}: unflagged points are drawn at \code{1.1}, confirmed batches at
\code{2.6}. It is a multiplier rather than an absolute size precisely so that
enlarging the marks keeps the flagged ones bigger than the rest.}

\item{grid_linewidth}{Line width of the zero lines and the dashed
significance thresholds this function draws -- the package's own reference
grid, not \pkg{ggplot2}'s. Default \code{0.3}.}

\item{palette}{A named colour palette (see \code{\link[=tbl_now_palette]{tbl_now_palette()}}).}
}
\value{
A \pkg{ggplot2} object (or a \pkg{plotly} widget when \code{plotly = TRUE}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Places each report date by its creation score (x) and transport / deficit
score (y) from \code{\link[=transport_discriminant]{transport_discriminant()}}, shading the region that decides the
batch call. Surges are not distinguished here (they fold into the quiet
background) since only the batch call is of interest.
}
\details{
Only the \code{\link[=diagnose_batches]{diagnose_batches()}}-confirmed batches (Benjamini-Hochberg-corrected) are
coloured red; the dashed lines and shaded region are a reference for where a
batch sits (deficit cleared, and significant), not the flagging rule. The most
extreme-looking points (far left, far up) are \emph{holds} -- windows still depleted
because the release has not happened yet -- not batches. A genuine batch sits in
the band just to the right of the vertical line, once the window total recovers.
}
\examples{
data(denguedat)
dn <- tbl_now(denguedat, onset_week, report_week, verbose = FALSE)
plot_transport_discriminant(dn)
}
\seealso{
\code{\link[=transport_discriminant]{transport_discriminant()}} for the numbers behind the plane;
\code{\link[=diagnose_batches]{diagnose_batches()}} for the hypothesis test that flags the red points;
\link[=plot_epidemic_process]{plot_reporting_process()} for the series they come
from; \code{\link[=diagnostic_plot]{diagnostic_plot()}} for the whole gallery.
}
