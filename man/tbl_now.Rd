% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tbl_now.R
\name{tbl_now}
\alias{tbl_now}
\title{Create a \code{tbl_now} object}
\usage{
tbl_now(
  data,
  event_date = NULL,
  report_date = NULL,
  delay = NULL,
  strata = NULL,
  covariates = NULL,
  case_count = NULL,
  is_censored = NULL,
  validation_date = NULL,
  validation_type = NULL,
  validation_units = "auto",
  now = NULL,
  event_units = "auto",
  report_units = "auto",
  data_type = "auto",
  t_effects = character(0),
  verbose = TRUE,
  force = FALSE,
  warn_non_uniqueness = TRUE,
  align_weeks = FALSE,
  ...
)
}
\arguments{
\item{data}{A \code{data.frame} or \code{tibble} to be converted.}

\item{event_date}{\href{https://dplyr.tidyverse.org/reference/dplyr_tidy_select.html}{tidy-select}
name of the column containing the event date. Optional when \code{delay} is
provided together with \code{report_date}; the event date will be computed as
\code{report_date - delay}.}

\item{report_date}{\href{https://dplyr.tidyverse.org/reference/dplyr_tidy_select.html}{tidy-select}
name of the column containing the report date. Optional when \code{delay} is
provided together with \code{event_date}; the report date will be computed as
\code{event_date + delay}.}

\item{delay}{(optional) \href{https://dplyr.tidyverse.org/reference/dplyr_tidy_select.html}{tidy-select}
or \code{NULL} (default). Name of a numeric column containing the delay (in
\code{event_units}) between \code{event_date} and \code{report_date}. When provided with
only one of \code{event_date} or \code{report_date}, the missing date is reconstructed
from the known date and the delay. Requires units to be known (either
specified via \code{event_units} or inferrable from the provided date column).}

\item{strata}{(optional) \href{https://dplyr.tidyverse.org/reference/dplyr_tidy_select.html}{tidy-select}
or \code{NULL} (default). Name of different variables (column names) in strata.
Strata correspond to variables that are of interest by themselves.
For example if it is of interest to generate nowcasts by gender then
\code{gender} is a \code{strata}.}

\item{covariates}{(optional) \href{https://dplyr.tidyverse.org/reference/dplyr_tidy_select.html}{tidy-select}
or \code{NULL} (default). Name of different variables
(column names) that influence the nowcast but are not strata.
For example precipitation might influence a dengue nowcast but in general it
is not of interest to generate nowcasts by precipitation levels.}

\item{case_count}{(optional) \href{https://dplyr.tidyverse.org/reference/dplyr_tidy_select.html}{tidy-select} or \code{NULL}
Name of the column with the case counts if \code{data_type} is "count-incidence"
or "count-cumulative".}

\item{is_censored}{(optional)
\href{https://dplyr.tidyverse.org/reference/dplyr_tidy_select.html}{tidy-select} or \code{NULL} (default).
The name of a column containing either \code{TRUE} or \code{FALSE} indicating whether
the \code{report_date} is correctly specified or corresponds to a \code{batch} and thus
is censored. In other words, if the \code{report_date} is accurately measured
set \code{is_censored = FALSE} but if the \code{report_date} corresponds to an error
and is only an upper bound of the real report date
set \code{is_censored = TRUE}.}

\item{validation_date}{(optional)
\href{https://dplyr.tidyverse.org/reference/dplyr_tidy_select.html}{tidy-select}
column holding a \strong{third} date: the day the report was resolved. Influenza is
the picture to keep in mind -- symptoms begin (the event), the patient sees a
doctor (the report), and days later a swab comes back. The assumed timeline is
\verb{event_date <= report_date <= validation_date <= now}. Leave \code{NULL} (the
default) for the usual two-date object. See \code{\link[=add_validation_date]{add_validation_date()}}.}

\item{validation_type}{(optional)
\href{https://dplyr.tidyverse.org/reference/dplyr_tidy_select.html}{tidy-select}
column saying what the resolution \emph{was}: \code{"confirmed"}, \code{"retracted"} (it was
reported, but it is not a case after all), \code{"pending"} or \code{NA}. \strong{\code{"pending"}
means reported and still waiting}, so it carries no validation date --
which is a different thing from a result that was never recorded (\code{NA}). A
validation date with no type warns rather than guessing, because a date
alone cannot say whether the case was confirmed or retracted.}

\item{validation_units}{(optional) Character. Either \code{"auto"} (default),
\code{"days"}, \code{"weeks"}, \code{"months"}, \code{"years"} or \code{"numeric"} -- the grid the
validation date lives on, resolved the same way as \code{report_units}.}

\item{now}{(optional) Date or \code{NULL} (default). The date that is considered the \code{now} of the
nowcast. If no \code{now} is given then the function automatically uses the last
\code{event_date}.}

\item{event_units}{(optional) Character. Either "auto" (default), "days",
"weeks", "months", "years" or "numeric".}

\item{report_units}{(optional) Character. Either "auto" (default), "days",
"weeks", "months", "years" or "numeric".}

\item{data_type}{(optional) Character. Either "auto", "linelist" or
"count-incidence" or "count-cumulative". See section below for
an explanation on data types.}

\item{t_effects}{(optional) Either \code{NULL} (default), a \code{\link[=temporal_effects]{temporal_effects()}} object
or a character vector with the names of the columns containing the temporal effects.}

\item{verbose}{(optional) Logical. Whether to throw a message. Default = \code{TRUE}.}

\item{force}{(optional) Logical. Whether to force computation overwriting pre-existing variables.
Default = \code{FALSE}.}

\item{warn_non_uniqueness}{(optional) Logical. Whether to throw a warning if data
has multiple observations for same event and report date (conditional on covariates
and strata)}

\item{align_weeks}{(optional) Logical. If both event and report units are weeks
and \code{align_weeks = TRUE} it ensures that all weeks start in a Sunday so that
week differences and \code{.delays} are all integer.}

\item{...}{Additional metadata to be stored as attributes on the object. Use
this for provenance you want to travel with the data -- \code{data_source},
\code{citation}, \code{population} -- and read it back with \code{\link[=tbl_now_attributes]{tbl_now_attributes()}}.

Because anything unmatched lands here, a misspelled argument name would
otherwise be accepted in silence. Names close enough to a real argument to be
a typo (\code{case_col} for \code{case_count}, \code{stata} for \code{strata}) warn instead; the
warning is safe to ignore if the name really was metadata.}
}
\value{
An object of class \code{tbl_now}: the input \code{data} as a \code{tibble}, carrying
extra attributes that record which columns hold the event date, report date,
strata, covariates and so on, plus the \code{now} of the nowcast. List them with
\code{\link[=tbl_now_attributes]{tbl_now_attributes()}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Surveillance data arrives late. A case that happened on Monday may only reach
the surveillance system on Thursday, so counts for the most recent days always
look artificially low. \emph{Nowcasting} corrects that artifact: it estimates how
many cases have already happened but have not been reported yet.

To do that, a model needs two dates for every case -- when it \strong{happened}
(\code{event_date}) and when it was \strong{reported} (\code{report_date}) -- together with
the date you are standing on (\code{now}). \code{tbl_now()} takes an ordinary
\code{data.frame} and records which of its columns play those roles, so you only
have to say it once.

The result still behaves like a \code{tibble}: \code{dplyr} verbs, \code{$}, \code{[} and
\code{ggplot2} keep working, and every \code{tbl.now} function knows where to find the
dates without being told again.
}
\details{
The minimum you must supply is \code{event_date} and \code{report_date} (or one of them
plus a \code{delay} column, from which the other is reconstructed). Everything else
is optional and can be added later with \code{\link[=add_strata]{add_strata()}}, \code{\link[=add_covariates]{add_covariates()}},
\code{\link[=add_validation_date]{add_validation_date()}} and the rest of the \code{\link[=add]{add()}} family.

Once the object exists the usual path is \link[=tbl_now_summary]{summary()} to see
what is in the data, \code{\link[=diagnose]{diagnose()}} to see what is wrong with it,
\link[=autoplot.tbl_now]{autoplot()} to look at it, and \code{\link[=run_nowcast]{run_nowcast()}} to fit a
model. \code{vignette("tbl.now")} walks through that path end to end.
}
\section{Attributes}{


The following attributes are part of a \code{tbl_now} and are validated
by the \code{\link[=validate_tbl_now]{validate_tbl_now()}} function:

\describe{
\item{event_date}{Name of the column refering to the event of interest.}
\item{report_date}{Name of the column refering to when the event of interest was reported.}
\item{strata}{Names of the columns corresponding to the strata (for modelling).}
\item{covariates}{Names of the columns corresponding to covariates (for modelling).}
\item{case_count}{Column containing the number of observations for that moment if \code{data_type} is \code{count-incidence} or \code{count-cumulative}.}
\item{temporal_effects}{Names of the columns refering to the temporal effects.}
\item{now}{Date of the \code{now} for a nowcast.}
\item{is_censored}{Column indicating whether the measurement is noisy (only upper bound) or not.}
\item{event_units}{Either \code{days}, \code{weeks}, \code{months}, \code{years} or \code{numeric}. Corresponds to the units of \code{event_date}}
\item{report_units}{Either \code{days}, \code{weeks}, \code{months}, \code{years} or \code{numeric}. Corresponds to the units of \code{report_date}}
\item{data_type}{Either \code{linelist}, \code{count-incidence} or \code{count-cumulative} depending on whether it is linelist data
or count data with incidence (each report date's incidence) or cumulative (overall known cases at report date)}
\item{validation_date}{Name of the column with the (optional) third date: when the report was resolved.}
\item{validation_type}{Name of the column saying what that resolution was (\code{"confirmed"}, \code{"retracted"}, \code{"pending"}).}
\item{validation_units}{Units of \code{validation_date}, resolved like \code{report_units}.}
\item{computed_temporal_effect_cols}{Names of the temporal-effect columns that have actually been materialised in the data by \code{\link[=compute_temporal_effects]{compute_temporal_effects()}}.}
}

You can  list all \code{tbl_now} related attributes in a specific \code{tbl_now} with \code{\link[=tbl_now_attributes]{tbl_now_attributes()}}.
}

\section{Data types}{

The following data-types are admitted at \code{tbl_now} objects.

\emph{Linelist}

Each row is an individual that was reported at \code{report_date} as happening at \code{event_date}.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{df <- data.frame(
 patient     = 1:6,
 event_date  = c(rep(as.Date("2020/09/12"), 3),
                 rep(as.Date("2020/09/13"), 3)),
 report_date = c(as.Date("2020/09/12"),
                 as.Date("2020/09/13"),
                 as.Date("2020/09/14"),
                 as.Date("2020/09/13"),
                 as.Date("2020/09/14"),
                 as.Date("2020/09/15")))
print(df)
#>   patient event_date report_date
#> 1       1 2020-09-12  2020-09-12
#> 2       2 2020-09-12  2020-09-13
#> 3       3 2020-09-12  2020-09-14
#> 4       4 2020-09-13  2020-09-13
#> 5       5 2020-09-13  2020-09-14
#> 6       6 2020-09-13  2020-09-15
}\if{html}{\out{</div>}}

\emph{Count-incidence}

Each \code{report_date}-\code{event_date} combination contains the total number of
cases observed \emph{exactly} at \code{report_date} for \code{event_date}.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{df <- data.frame(
 n           = c(7, 1, 9, 5, 0, 2),
 event_date  = c(rep(as.Date("2020/09/12"), 3),
                 rep(as.Date("2020/09/13"), 3)),
 report_date = c(as.Date("2020/09/12"),
                 as.Date("2020/09/13"),
                 as.Date("2020/09/14"),
                 as.Date("2020/09/13"),
                 as.Date("2020/09/14"),
                 as.Date("2020/09/15")))
print(df)
#>   n event_date report_date
#> 1 7 2020-09-12  2020-09-12
#> 2 1 2020-09-12  2020-09-13
#> 3 9 2020-09-12  2020-09-14
#> 4 5 2020-09-13  2020-09-13
#> 5 0 2020-09-13  2020-09-14
#> 6 2 2020-09-13  2020-09-15
}\if{html}{\out{</div>}}

\emph{Count-cumulative}

Each \code{report_date}-\code{event_date} combination contains the total number of
cases observed up until \code{report_date} for \code{event_date}. The most recent
\code{report_date} contains the best estimation of cases happening at \code{event_date}.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{df <- data.frame(
 n           = c(1,5, 8, 2, 2, 4),
 event_date  = c(rep(as.Date("2020/09/12"), 3),
                 rep(as.Date("2020/09/13"), 3)),
 report_date = c(as.Date("2020/09/12"),
                 as.Date("2020/09/13"),
                 as.Date("2020/09/14"),
                 as.Date("2020/09/13"),
                 as.Date("2020/09/14"),
                 as.Date("2020/09/15")))
print(df)
#>   n event_date report_date
#> 1 1 2020-09-12  2020-09-12
#> 2 5 2020-09-12  2020-09-13
#> 3 8 2020-09-12  2020-09-14
#> 4 2 2020-09-13  2020-09-13
#> 5 2 2020-09-13  2020-09-14
#> 6 4 2020-09-13  2020-09-15
}\if{html}{\out{</div>}}

The \code{\link[=to_count]{to_count()}} function allows you to easily convert from between different
data-types.
}

\examples{
# `denguedat` is a linelist: one row per dengue case, with the week symptoms
## began (`onset_week`) and the week the case reached the surveillance system
## (`report_week`).
data(denguedat)
head(denguedat)

# Tell tbl.now which column plays which role. `now` defaults to the last
# event date seen in the data.
ndata <- tbl_now(denguedat,
  event_date = onset_week,
  report_date = report_week,
  strata = gender
)

# Printing reports back the roles it recorded, and the `now` it chose.
ndata

# A `tbl_now` is still a tibble, so ordinary manipulation works ...
ndata$newcolumn <- "something"
ndata[1:10, ]

# ... including dplyr verbs.
ndata |>
  dplyr::filter(report_week <= as.Date("1991-01-02"))

# Dropping a strata column simply forgets that stratum.
ndata |> dplyr::select(-gender)

# But dropping a column the class depends on demotes the object back to a
## plain tibble (with a warning): without an event date it can no longer
# describe a nowcast.
suppressWarnings(
  ndata |> dplyr::select(-onset_week)
)

}
\seealso{
\code{\link[=as_tbl_now]{as_tbl_now()}} to convert an object created by another nowcasting package;
\code{\link[=to_count]{to_count()}} to move between linelist and aggregated count data;
\code{\link[=tbl_now_attributes]{tbl_now_attributes()}} to list what the object recorded;
\code{\link[=validate_tbl_now]{validate_tbl_now()}} and \code{\link[=diagnose]{diagnose()}} to check it;
\link[=tbl_now_summary]{summary()} to describe it;
\link[=autoplot.tbl_now]{autoplot()} to plot it;
\code{\link[=run_nowcast]{run_nowcast()}} to fit a nowcast.
}
