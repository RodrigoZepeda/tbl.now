% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/autoplot.R
\name{autoplot.tbl_now}
\alias{autoplot.tbl_now}
\title{Diagnostic \code{autoplot} for a \code{tbl_now}}
\usage{
\method{autoplot}{tbl_now}(
  object,
  ...,
  level = 0.95,
  palette = .tbl_now_palette(),
  delay_distribution_xlim = NULL,
  event_date_xlim = NULL,
  calendar_effect_xlim = NULL,
  seasonality_xlim = NULL
)
}
\arguments{
\item{object}{A \code{tbl_now} object.}

\item{...}{Unused; present for compatibility with \code{\link[ggplot2:autoplot]{ggplot2::autoplot()}}.}

\item{level}{Completeness level used for the incompleteness line in panel 2.
The line is drawn at \code{now - q}, where \code{q} is the \code{level} quantile of the
delay distribution. With the default \code{0.95}, the line marks where at least
5 percent of delays are yet to arrive.}

\item{palette}{A named character vector of colours. Defaults to the package
palette.}

\item{delay_distribution_xlim, event_date_xlim, calendar_effect_xlim, seasonality_xlim}{Optional length-2 vectors giving the x-axis limits for the corresponding
panel (delay density, epidemic process, calendar effect, periodogram).
\code{NULL} (default) lets each panel pick its own range. For \code{event_date_xlim}
pass \code{Date}s; the others take numeric limits.}
}
\value{
A \pkg{patchwork} object combining the four panels.
}
\description{
Produces a four-panel diagnostic overview of a \code{tbl_now} using
\pkg{ggplot2} and \pkg{patchwork}:
\enumerate{
\item \strong{Empirical delay distribution} — a (case-count weighted) kernel
density of the reporting delay (\code{.delay}).
\item \strong{Observed epidemic process} — the latest reported case counts per
\code{event_date}, with a dashed vertical line marking where the data become
incomplete (less than \code{level} of the delay distribution has arrived).
\item \strong{Calendar effect} — boxplots of the \emph{normalized} effect (each event
date's cases divided by the overall mean, so 1 is average) by calendar
group. Daily data shows \strong{both} a day-of-week and a week-of-year panel;
weekly data shows week-of-year; monthly data shows month-of-year.
\item \strong{Seasonality} — a periodogram of the incidence series whose dominant
peak suggests a Fourier season length for \code{\link[=temporal_effects]{temporal_effects()}}.
}

If the attached \code{\link[=temporal_effects]{temporal_effects()}} spec contains a holidays calendar, the
event dates that fall on a holiday are marked with red dots in panel 2.
}
\details{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\examples{
\dontshow{if (requireNamespace("patchwork", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(denguedat)
dengue <- tbl_now(denguedat, event_date = "onset_week",
                  report_date = "report_week", verbose = FALSE)
autoplot(dengue)

# Zoom the delay panel to delays of 0-10 weeks
if (FALSE){
  autoplot(dengue, delay_distribution_xlim = c(0, 10))
}
\dontshow{\}) # examplesIf}
}
