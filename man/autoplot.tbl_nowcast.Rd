% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_plot.R
\name{autoplot.tbl_nowcast}
\alias{autoplot.tbl_nowcast}
\title{Plot a nowcast}
\arguments{
\item{object}{A \link{tbl_nowcast} object.}

\item{...}{Unused; present for compatibility with \code{\link[ggplot2:autoplot]{ggplot2::autoplot()}}.}

\item{levels}{Numeric vector of central interval widths to shade. Defaults to
the widest intervals available in the object.}

\item{show_reported}{Logical. Whether to draw the cases \strong{reported so far}
as columns under the fan -- \code{\link[=get_latest_reported_cases]{get_latest_reported_cases()}} on the object's own
source data, so the bars are what the model was actually shown as of \code{now},
not what those dates eventually reached. The vertical gap between the top of
a bar and the fan is the correction the nowcast is making, which is the
whole reason to draw it. Requires the nowcast to carry its source data.

The bars are one period wide, taken from \code{\link[=get_event_units]{get_event_units()}}. A fixed width
would draw hairlines on a weekly series, and one wider than the step would
make \pkg{ggplot2} stack overlapping bars, so each would show several
periods' counts rather than its own.}

\item{colour}{Colour of the fan. Defaults to the \code{tbl.now} palette's green:
a nowcast is an estimate of the \strong{epidemic} process (cases by event date),
which the package always draws in green, with red reserved for the
reporting process.}

\item{date_lim}{Length-2 vector of event-axis limits, as \code{Date}s (or as
numbers on a numeric event axis). \code{NA} in either position leaves that end
alone. A nowcast covers the whole series but only \emph{corrects} its final
periods, so the interesting part is usually the last few weeks; this zooms
on to them.

The limits are applied with \code{\link[ggplot2:coord_cartesian]{ggplot2::coord_cartesian()}}, so they \strong{crop}
the drawn plot rather than filter the data. That matters here: a scale
limit would drop the out-of-range rows before the ribbon is built, which
cuts the fan off at the boundary instead of letting it run to the edge.}

\item{ylim}{Length-2 vector of count-axis limits, applied the same way.
\code{NULL} (default) leaves the axis to \pkg{ggplot2}. Note that a stratified
nowcast facets with \code{scales = "free_y"}, so one pair of limits is imposed
on every panel.}
}
\value{
A \code{ggplot} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Draws a fan chart of a \link{tbl_nowcast}: the counts reported so far as grey
columns, one shaded band per central prediction interval over them, and the
median as a line, so that the size of the correction the model is applying is
visible as the gap between the bars and the fan.
}
\examples{
predictions <- tidyr::expand_grid(
  onset_week = as.Date("2020-01-05") + seq(0, 28, by = 7),
  .quantile_level = c(0.05, 0.25, 0.5, 0.75, 0.95)
)
predictions$.value <- 10 + 30 * predictions$.quantile_level
nc <- tbl_nowcast(predictions = predictions, method = "toy", event_date = "onset_week")

autoplot(nc)

# Zoom on to the corrected weeks without dropping the rows that build the fan.
autoplot(nc, date_lim = c(as.Date("2020-01-19"), as.Date("2020-02-02")))

}
\seealso{
\link{tbl_now_workflows} for the native/common workflow boundary;
\code{\link[=run_nowcast]{run_nowcast()}}, \code{\link[=nowcast_ensemble]{nowcast_ensemble()}}
}
