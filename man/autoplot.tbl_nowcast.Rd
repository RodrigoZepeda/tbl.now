% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_plot.R
\name{autoplot.tbl_nowcast}
\alias{autoplot.tbl_nowcast}
\title{Plot a nowcast}
\arguments{
\item{object}{A \link{tbl_nowcast} object.}

\item{...}{Unused; present for compatibility with \code{\link[ggplot2:autoplot]{ggplot2::autoplot()}}.}

\item{levels}{Numeric vector of central interval widths to shade. Defaults to
the widest intervals available in the object.}

\item{show_reported}{Logical. Whether to overlay the cases reported up to
\code{now}. Requires the nowcast to carry its source data.}

\item{colour}{Colour of the fan. Defaults to the \code{tbl.now} palette's green:
a nowcast is an estimate of the \strong{epidemic} process (cases by event date),
which the package always draws in green, with red reserved for the
reporting process.}
}
\value{
A \code{ggplot} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Draws a fan chart of a \link{tbl_nowcast}: one shaded band per central prediction
interval, the median as a line, and the counts reported so far as points, so
that the size of the correction the model is applying is visible.
}
\examples{
predictions <- tidyr::expand_grid(
  onset_week = as.Date("2020-01-05") + seq(0, 28, by = 7),
  .quantile_level = c(0.05, 0.25, 0.5, 0.75, 0.95)
)
predictions$.value <- 10 + 30 * predictions$.quantile_level
nc <- tbl_nowcast(predictions = predictions, method = "toy", event_date = "onset_week")

autoplot(nc)

}
\seealso{
\code{\link[=run_nowcast]{run_nowcast()}}, \code{\link[=nowcast_ensemble]{nowcast_ensemble()}}
}
