% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/delay_drift.R
\name{test_delay_changepoint}
\alias{test_delay_changepoint}
\title{Detect an abrupt change point in the reporting-delay distribution}
\usage{
test_delay_changepoint(
  x,
  ...,
  stat = c("median", "spread", "mean", "iqr"),
  by_strata = FALSE,
  strata = NULL,
  mature_only = TRUE,
  level = 0.95,
  alpha = 0.05
)
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{...}{Passed to the underlying \pkg{modifiedmk} function (e.g. \code{nsim}
for \code{"block-bootstrap"}).}

\item{stat}{Which delay summaries to test: any of \code{"median"}, \code{"mean"},
\code{"iqr"} (q75 - q25) and \code{"spread"} (q90 - q10). Defaults to median + spread,
i.e. one \emph{location} and one \emph{dispersion} statistic.}

\item{by_strata}{Logical (default \code{FALSE}). When \code{TRUE}, the test is run
separately per stratum.}

\item{strata}{Character vector of columns to group on when
\code{by_strata = TRUE}. \code{NULL} (default) uses the object's \code{strata}.}

\item{mature_only}{Logical (default \code{TRUE}). Drop event dates after the
\code{level} incompleteness cutoff before testing.}

\item{level}{Completeness level for the maturity cutoff (default \code{0.95}).}

\item{alpha}{Significance level for the \code{drift} verdict column (default
\code{0.05}).}
}
\value{
A tibble with one row per \code{stat} x stratum: \code{strata}, \code{stat},
\code{n} (series length), \code{changepoint} (the event date of the estimated change,
\code{NA} when none is found), \code{statistic} (Pettitt's \code{K}), \code{p_value},
\code{before} / \code{after} (the mean of the statistic on each side of the change),
\code{shift} (\code{after - before}) and a logical \code{changepoint_detected}
(\code{p_value < alpha}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Complements \code{\link[=test_delay_drift]{test_delay_drift()}}. Where that tests for a \emph{gradual} monotonic
trend, this tests for a \strong{single abrupt shift} (e.g. a reporting-system change
on some date) in the per-period delay summaries, using \strong{Pettitt's}
nonparametric change-point test. As with \code{\link[=test_delay_drift]{test_delay_drift()}} it works on both
a location statistic (median / mean) and a dispersion statistic (IQR / 10-90
spread), on mature data only, and — being rank-based — it is robust to the
skew and serial dependence of a delay series.
}
\examples{
data(denguedat)
dengue <- tbl_now(denguedat,
  event_date = "onset_week", report_date = "report_week", verbose = FALSE
)
test_delay_changepoint(dengue)

}
\seealso{
\code{\link[=test_delay_drift]{test_delay_drift()}}, \code{\link[=plot_delay_drift]{plot_delay_drift()}}
}
