% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose.R
\name{print.tbl_now_diagnosis}
\alias{print.tbl_now_diagnosis}
\title{Print a \code{tbl_now} diagnosis}
\usage{
\method{print}{tbl_now_diagnosis}(x, ..., all = FALSE)
}
\arguments{
\item{x}{A findings tibble, from \code{\link[=diagnose]{diagnose()}} or one of the
\link{nowcast_diagnose_components}.}

\item{...}{Unused.}

\item{all}{Logical. Spell out the \code{ok} and \code{skipped} findings too, instead of
counting them. Defaults to \code{FALSE}, and to \code{TRUE} when there is nothing
else to report -- a block that found nothing wrong would otherwise print an
empty report.}
}
\value{
\code{x}, invisibly.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Prints the findings \code{\link[=diagnose]{diagnose()}} returned as a report: the errors, warnings
and notes in full, each with its hint, and the checks that passed and that
could not be assessed as one line each.

The object is an ordinary tibble underneath, so
\code{print(tibble::as_tibble(x))} gives the table and every \code{dplyr} verb still
works on it.
}
\examples{
data(denguedat)
ndata <- tbl_now(denguedat,
  event_date = "onset_week", report_date = "report_week",
  strata = "gender", verbose = FALSE
)

diagnose(ndata)

# Every finding, including the ones that passed.
print(diagnose(ndata), all = TRUE)

# Still a tibble.
print(tibble::as_tibble(diagnose(ndata)))

}
\seealso{
\code{\link[=diagnose]{diagnose()}}, \link{nowcast_diagnose_components}
}
