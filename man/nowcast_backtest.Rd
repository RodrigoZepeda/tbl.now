% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_score.R
\name{nowcast_backtest}
\alias{nowcast_backtest}
\title{Refit several methods at past \code{now} dates and score them}
\usage{
nowcast_backtest(
  x,
  ...,
  now_dates = NULL,
  horizon = 4,
  n_dates = 4L,
  seed = NULL,
  keep_draws = FALSE,
  on_error = c("warn", "abort"),
  verbose = TRUE,
  truth_axis = c("report", "revision"),
  truth_type = "total"
)
}
\arguments{
\item{x}{A \code{tbl_now} object holding the \emph{full} data (the later reports or
revisions are what the retrospective nowcasts are scored against).
Covariates on each retrospective snapshot should mean values available as
of that snapshot's \code{now}; do not attach future realized covariate values
unless they are an explicit forecast input for the engine.}

\item{...}{The \code{\link[=engine]{engine()}} objects to backtest, one per model. Each carries its
own arguments, so there is no keyed side-table of per-method options to get
wrong.

Give an engine a \code{label} (or name the argument) when the same package
appears twice: \code{engine_diseasenowcasting(label = "ar1", model = ...)} and a
plain \code{engine_diseasenowcasting()} are backtested separately, so
\code{\link[=nowcast_weights]{nowcast_weights()}} can learn a weight for each -- matching how
\code{\link[=nowcast_ensemble]{nowcast_ensemble()}} takes a named list of members. An engine with no label
is labelled by its method.}

\item{now_dates}{Vector of retrospective nowcast origins. Defaults to the
\code{n_dates} most recent report-axis dates that are at least \code{horizon} units
before the object's \code{now}; these dates are used as as-of origins, not as a
filter on target event dates.}

\item{horizon}{Number of time units of hindsight required when \code{now_dates} is
chosen automatically. Default \code{4}.}

\item{n_dates}{Number of automatic retrospective origins. Default \code{4}.
Ignored when \code{now_dates} is supplied explicitly.}

\item{seed}{Optional integer. When given, the RNG is seeded \strong{immediately
before each fit}, from \code{seed} and the label and date that fit is for. One
\code{set.seed()} before the whole backtest is not enough: it only pins anything
if every method consumes the same random numbers in the same order, so
dropping a method, or refitting one date, silently moves every other fit.
Seeding per (label, date) makes a fit depend only on which fit it is.}

\item{keep_draws}{Logical. Whether to retain every posterior draw from every
successful fit. Default \code{FALSE}, because this can make a backtest much
larger. Set it to \code{TRUE} when the backtest should be passed directly to
\code{\link[scoringutils:as_forecast_sample]{scoringutils::as_forecast_sample()}}. Engines that return only quantiles
still cannot be converted to samples.}

\item{on_error}{Either \code{"warn"} (default) to skip a model/date that fails
with a warning, or \code{"abort"} to stop.}

\item{verbose}{Logical. Whether to report progress.}

\item{truth_axis}{Which process defines the observed counts. \code{"report"}
(default) scores counts eventually reported. \code{"revision"} scores counts
eventually resolved on the revision axis and requires a revision-aware
\code{truth}.}

\item{truth_type}{Which case type to score. Defaults to \code{"total"}. Revision
types such as \code{"confirmed"}, \code{"retracted"}, \code{"pending"}, \code{"unknown"} and
\code{"net"} follow the same meanings as \code{\link[=get_latest_reported_cases]{get_latest_reported_cases()}} and
\code{\link[=get_latest_revised_cases]{get_latest_revised_cases()}}. \code{"by_type"} is refused because scoring needs
one observed value per event-date/stratum target.}
}
\value{
An object of class \code{nowcast_backtest}: a list with

\describe{
\item{scores}{A \code{tibble} of per-date scores with an extra \code{.now} column.}
\item{predictions}{A \code{tibble} of every retrospective quantile prediction.}
\item{draws}{When \code{keep_draws = TRUE}, a \code{tibble} of the retained draws;
otherwise \code{NULL}.}
\item{timings}{A \code{tibble} with one row per attempted engine/date fit,
its elapsed time in seconds, whether it succeeded, and any error text.}
\item{truth}{The observed counts used for scoring.}
\item{methods}{The labels that produced at least one nowcast.}
\item{now_dates}{The dates that were nowcast.}
}
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

Walks back through time: for every date in \code{now_dates}, the \code{tbl_now} is
truncated to the reports that were available then, each method is refitted on
that snapshot, and the resulting nowcast is scored against the resolved truth
defined by \code{truth_axis} and \code{truth_type} (reported totals by default). This is
what turns a set of models into ensemble weights (see \code{\link[=nowcast_weights]{nowcast_weights()}} and
\code{\link[=nowcast_ensemble]{nowcast_ensemble()}}).

Be aware that this refits every model once per date: with Bayesian backends
and a long \code{now_dates} it is genuinely expensive.
}
\section{Use the result directly with scoringutils}{


A \code{nowcast_backtest} has methods for
\code{\link[scoringutils:as_forecast_quantile]{scoringutils::as_forecast_quantile()}},
\code{\link[scoringutils:as_forecast_point]{scoringutils::as_forecast_point()}}, and
\code{\link[scoringutils:as_forecast_sample]{scoringutils::as_forecast_sample()}}, so no manual reshaping is needed:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{quantile_forecast <- scoringutils::as_forecast_quantile(bt)
point_forecast <- scoringutils::as_forecast_point(bt)
}\if{html}{\out{</div>}}

For sample forecasts, create the backtest with \code{keep_draws = TRUE} and
use \code{scoringutils::as_forecast_sample(bt)}. The returned forecast objects can
be passed to any compatible scoringutils workflow. For example, relative WIS
is obtained with:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{relative_scores <- quantile_forecast |>
  scoringutils::score() |>
  scoringutils::add_relative_skill(metric = "wis")
}\if{html}{\out{</div>}}

\code{model}, \code{now}, the event-date column, and declared strata are retained as
forecast units, allowing scores to be extended, regrouped, or summarised
without returning to the internal \code{tbl.now} representation.
}

\section{Every engine must report the same quantile levels}{


A backtest exists to compare models, and two models summarised at different
levels are not comparable: the weighted interval score is an average over the
levels reported, so a model asked for three of them and one asked for nine are
scoring different quantities. Mismatched engines are therefore an \strong{error}
rather than a warning.

This matters most for the engines where the levels are a \emph{fit-time} argument.
\pkg{NobBS} computes exactly the quantiles it is handed and keeps no draws, so
a level it was never asked for cannot be recovered afterwards -- and an
ensemble weighted from such a backtest would silently fall back to whatever
levels its members happened to share.
}

\examples{
data(denguedat)

# A short recent window keeps the example quick.
recent <- subset(denguedat, onset_week >= as.Date("2010-06-01"))
dengue <- tbl_now(recent,
  event_date = onset_week, report_date = report_week, verbose = FALSE
)

## `example_engine()` is a toy that ignores the reporting delay entirely; it
# is used here only so the example runs without a modelling package.
## Swap in a real one -- `engine_baselinenowcast()`, `engine_epinowcast()`,
## `engine_nobbs()` -- for anything you intend to act on.

# Refit at two past `now` dates and score each against what is known now.
bt <- nowcast_backtest(dengue,
  example_engine(label = "carry forward"),
  now_dates = as.Date(c("2010-10-04", "2010-11-15")),
  verbose = FALSE
)
head(bt$scores)

# Naming several engines compares them on identical data and dates.
bt$methods

# With a real model the call is the same, with a real engine.
if (requireNamespace("baselinenowcast", quietly = TRUE)) {
  nowcast_backtest(dengue,
    engine_baselinenowcast(draws = 100),
    now_dates = as.Date("2010-11-15"), verbose = FALSE
  )$scores
}

}
\seealso{
\link{tbl_now_workflows} for choosing native model construction or cross-engine
comparison;
\code{\link[=engine]{engine()}} to specify each model being compared, and its \code{min_date} argument,
which matters here because \code{now} moves between fits;
\code{\link[=score_nowcast]{score_nowcast()}} for the scores computed at each \code{now};
\code{\link[=nowcast_weights]{nowcast_weights()}} to turn the result into ensemble weights, and
\code{\link[=nowcast_ensemble]{nowcast_ensemble()}} to use them;
\code{\link[scoringutils:score]{scoringutils::score()}} and \code{\link[scoringutils:add_relative_skill]{scoringutils::add_relative_skill()}} for an
extensible scoring workflow. The
\href{https://rodrigozepeda.github.io/tbl.now/articles/ensemble-nowcasting.html}{\emph{One call, many models} article}
compares several packages this way.
}
