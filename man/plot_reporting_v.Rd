% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/v_triangle.R
\name{plot_reporting_v}
\alias{plot_reporting_v}
\title{Plot the reporting "V"}
\usage{
plot_reporting_v(
  x,
  max_delay = NULL,
  point_size = NULL,
  plotly = FALSE,
  palette = .tbl_now_palette()
)
}
\arguments{
\item{x}{A \code{\link[=tbl_now]{tbl_now()}} object.}

\item{max_delay}{Largest delay to draw. \code{NULL} (default) fills \emph{every} delay
that could have been observed (0 up to \code{now} minus the earliest event).}

\item{point_size}{Size of the cell markers. \code{NULL} (default) scales it to the
number of cells.}

\item{plotly}{If \code{TRUE}, return an interactive \pkg{plotly} widget instead of a
static plot. Default \code{FALSE}.}

\item{palette}{A named colour palette. Defaults to the package palette.}
}
\value{
A \pkg{ggplot2} object (or a \pkg{plotly} widget when \code{plotly = TRUE}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
The \strong{same information} as \code{\link[=plot_reporting_triangle]{plot_reporting_triangle()}} -- every (event date,
delay) cell of the reporting triangle -- drawn in \strong{rotated coordinates}, so
that report date runs straight up the page and the data opens into a \emph{V}. Where
\code{\link[=plot_reporting_triangle]{plot_reporting_triangle()}} uses square axes (event date across, delay up), this
view rotates them 45 degrees:
\itemize{
\item the vertical axis is \code{event + delay} = the \strong{report date} (the V opens
upward over report time);
\item the \strong{left arm} (delay 0, where event = report) is the \strong{event-date}
axis;
\item the \strong{right arm} (the earliest event) is the \strong{delay} axis, reading
\code{0} outward;
\item every \strong{horizontal} line is one report date.
}
A \strong{batch} -- a single report date releasing a pile of old cases -- is then a
single \strong{horizontal streak} across the V, where the reporting triangle would
show it as a diagonal. The whole observable triangle is filled: every (event
date, delay) that \emph{could} have been reported by \code{now} is drawn, pale blue where
nothing was reported (a genuine zero) and coloured where reports landed.
}
\examples{
data(denguedat)
dn <- tbl_now(denguedat, onset_week, report_week, verbose = FALSE)
plot_reporting_v(dn)

}
\seealso{
\code{\link[=plot_reporting_triangle]{plot_reporting_triangle()}} for the same data on square axes,
\code{\link[=diagnostic_plot]{diagnostic_plot()}}.
}
