% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy.epidist_fit}
\alias{tidy.epidist_fit}
\title{Tidy the delay distribution from an \pkg{epidist} fit}
\usage{
\method{tidy}{epidist_fit}(x, probs = NULL, level = 0.95, newdata = NULL, ...)
}
\arguments{
\item{x}{A fit from \code{\link[epidist:epidist]{epidist::epidist()}}.}

\item{probs}{Optional numeric vector of probabilities in \verb{[0, 1]}, adding one
\verb{q*} column each.}

\item{level}{Width of the reported interval. Defaults to \code{0.95}.}

\item{newdata}{Optional data frame passed to
\code{\link[epidist:predict_delay_parameters]{epidist::predict_delay_parameters()}}, for a fit with covariates in the
delay model (\code{formula = mu ~ 1 + gender}, say). \code{NULL} uses the fit's own
data.}

\item{...}{Unused, for generic consistency.}
}
\value{
A tibble, as described in \emph{Value}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\pkg{epidist} is the one supported package that does \strong{not} produce a
nowcast. It estimates the \strong{reporting-delay distribution}, so there are no
per-event-date case estimates to tidy and the columns
\code{\link[=tidy.nowcast]{tidy.nowcast()}} promises (\code{event_date}, \code{stratum}, ...) would all be
meaningless. This method therefore returns a different, delay-shaped table --
one row per distribution parameter rather than one row per date.
}
\section{Value}{


A \link[tibble:tibble]{tibble} with one row per parameter of the fitted delay
distribution and these columns:

\describe{
\item{\code{term}}{\code{character}. The parameter: the distribution's own parameters
(\code{mu}, \code{sigma}, ... -- whichever the \code{family} has) plus the derived
\code{mean} and \code{sd}, which are the numbers most people actually want.}
\item{\code{estimate}}{\code{numeric}. Posterior median.}
\item{\code{conf.low}, \code{conf.high}}{\code{numeric}. Interval bounds, following
\pkg{broom}'s naming.}
\item{\code{level}}{\code{numeric}. The width of that interval.}
\item{\code{engine}}{\code{character}. Always \code{"epidist"}.}
}

\code{probs} appends one \verb{q*} column per requested probability, exactly as it does
for the nowcast methods -- the fit exposes draws, so any quantile is real
rather than an approximation.
}

\section{Dispatch}{


\code{epidist()} returns an object of class \code{c("brmsfit", "epidist_fit")}, in that
order, so if \pkg{broom.mixed} is loaded its \code{tidy.brmsfit()} method matches
\strong{first} and you get raw \pkg{brms} parameters instead of this table. Call
\code{tidy.epidist_fit(fit)} explicitly when you want the delay distribution and
cannot be sure which method will win.
}

\examples{
\dontshow{if (requireNamespace("epidist", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# Fitting needs Stan and takes minutes, so the fit itself is wrapped in
# \donttest{}; everything above it runs.
data(denguedat)
# A short window: fitting a delay distribution does not need twenty years of
# data, and Stan is slow.
recent <- subset(denguedat, onset_week >= as.Date("2010-06-01"))
nowobj <- tbl_now(recent,
  event_date = "onset_week", report_date = "report_week", verbose = FALSE
)

# The conversion itself is quick, and is what tidy() will later summarise.
converted <- suppressWarnings(tbl_now_to_epidist(nowobj, verbose = FALSE))
head(converted)

\donttest{
fit <- converted |>
  epidist::as_epidist_marginal_model() |>
  epidist::epidist()

tidy(fit)
tidy(fit, probs = c(0.05, 0.95))
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=tidy.nowcast]{tidy.nowcast()}} for the case-count nowcast engines,
\code{\link[=tbl_now_to_epidist]{tbl_now_to_epidist()}} for the conversion.
}
