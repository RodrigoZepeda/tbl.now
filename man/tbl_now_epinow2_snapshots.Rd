% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/converters.R
\name{tbl_now_epinow2_snapshots}
\alias{tbl_now_epinow2_snapshots}
\alias{print.tbl_now_epinow2_snapshots}
\title{Snapshots of one series, as \pkg{EpiNow2} estimates truncation from}
\usage{
\method{print}{tbl_now_epinow2_snapshots}(x, ...)
}
\arguments{
\item{x}{A \code{tbl_now_epinow2_snapshots}.}

\item{...}{Ignored.}
}
\value{
\code{print()} returns \code{x} invisibly.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The object returned by
\code{tbl_now_to_EpiNow2(x, target = "estimate_truncation")}: a list of
\code{date}/\code{confirm} data frames, one per report date, plus the report dates
themselves so the object can be turned back into a \code{tbl_now}.

It is a \strong{thin} class -- still a list, so it can be handed to
\code{\link[EpiNow2:estimate_truncation]{EpiNow2::estimate_truncation()}} unchanged:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{snaps <- tbl_now_to_EpiNow2(x, target = "estimate_truncation")
EpiNow2::estimate_truncation(snaps)
}\if{html}{\out{</div>}}

The class exists because a bare list of \code{date}/\code{confirm} frames does not say
\emph{when} each snapshot was taken, and without that the reporting triangle cannot
be recovered from it. Printing also distinguishes it from the superficially
similar list \code{\link[EpiNow2:estimate_secondary]{EpiNow2::estimate_secondary()}} does \emph{not} take.
}
\seealso{
\code{\link[=tbl_now_to_EpiNow2]{tbl_now_to_EpiNow2()}}, \code{\link[=as_tbl_now]{as_tbl_now()}}
}
