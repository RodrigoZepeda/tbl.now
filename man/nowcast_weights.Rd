% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_score.R
\name{nowcast_weights}
\alias{nowcast_weights}
\title{Ensemble weights from a backtest}
\usage{
nowcast_weights(backtest, type = c("inverse_score", "optim", "equal"), ...)
}
\arguments{
\item{backtest}{A \code{nowcast_backtest} object.}

\item{type}{How to derive the weights:

\describe{
\item{\code{"inverse_score"} (default)}{\eqn{w_i \propto 1/\overline{WIS}_i}.
Cheap, robust, and never puts all the mass on one model.}
\item{\code{"optim"}}{The weights on the simplex that minimise the WIS of the
quantile-averaged ensemble over the training window. Better in
principle, but prone to overfitting when the window is short.}
\item{\code{"equal"}}{\eqn{w_i = 1/M}. Included so that the same code path can
produce the unweighted ensemble.}
}}

\item{...}{Unused.}
}
\value{
A named numeric vector of weights summing to 1.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Turns the retrospective scores of a \code{\link[=nowcast_backtest]{nowcast_backtest()}} into a vector of
weights for \code{\link[=nowcast_ensemble]{nowcast_ensemble()}}.
}
\examples{
data(denguedat)

# A short recent window keeps the example quick.
recent <- subset(denguedat, onset_week >= as.Date("2010-06-01"))
dengue <- tbl_now(recent,
  event_date = onset_week, report_date = report_week, verbose = FALSE
)

## `example_engine()` is a toy that ignores the reporting delay entirely; it
# is used here only so the example runs without a modelling package.
## Swap in a real one -- `engine_baselinenowcast()`, `engine_epinowcast()`,
## `engine_nobbs()` -- for anything you intend to act on.

# Two engines that differ in how wide they claim their intervals are.
bt <- nowcast_backtest(dengue,
  example_engine(spread = 0.2, label = "narrow"),
  example_engine(spread = 0.5, label = "wide"),
  now_dates = as.Date(c("2010-10-04", "2010-11-15")),
  verbose = FALSE
)

# Weights sum to one, and the better-scoring engine takes the larger share.
nowcast_weights(bt)
sum(nowcast_weights(bt))

## Hand them to nowcast_ensemble() to pool the nowcasts they came from.

}
\seealso{
\code{\link[=nowcast_backtest]{nowcast_backtest()}}, which produces the scores these weights come from;
\code{\link[=nowcast_ensemble]{nowcast_ensemble()}}, which consumes them;
\code{\link[=engine]{engine()}}'s \code{label} argument, which is what tells two configurations of the
same package apart in the result.
}
