% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_batches.R
\name{detect_report_batches}
\alias{detect_report_batches}
\title{Detect batch reporting (backlog dumps) on the report-date axis}
\usage{
detect_report_batches(
  x,
  ...,
  signals = c("volume", "delay"),
  threshold = 3,
  backlog_delay = NULL,
  backlog_quantile = 0.9,
  baseline_window = NULL,
  gap_window = 3,
  min_reports = 1,
  by_strata = FALSE,
  strata = NULL
)
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{...}{Unused.}

\item{signals}{Character vector of the signals to require (AND-ed). Any of
\code{"volume"}, \code{"delay"}, \code{"span"}, \code{"gap"}. Defaults to
\code{c("volume", "delay")}.}

\item{threshold}{Robust-z cutoff a signal must reach to fire (default \code{3}).}

\item{backlog_delay}{Delay at/above which a report counts as backlog (for the
\code{backlog} column). \code{NULL} (default) uses the \code{backlog_quantile} of the
observed delays.}

\item{backlog_quantile}{Delay quantile used to set \code{backlog_delay} when it is
\code{NULL} (default \code{0.9}).}

\item{baseline_window}{Rolling-baseline width in report periods. \code{NULL}
(default) uses roughly two months.}

\item{gap_window}{Number of preceding periods assessed by the \code{"gap"} signal
(default \code{3}).}

\item{min_reports}{Minimum reports for a period to be eligible (default \code{1}).}

\item{by_strata}{Logical (default \code{FALSE}). Detect batches separately per
stratum.}

\item{strata}{Character vector of columns to group on when \code{by_strata = TRUE}.
\code{NULL} (default) uses the object's \code{strata}.}
}
\value{
A tibble with one row per candidate report date (\code{n_reports > 0}),
carrying \code{report_date} (and \code{strata} when grouped), the features
(\code{n_reports}, \code{n_event_dates}, \code{mean_delay}, \code{median_delay}, \code{max_delay},
\code{backlog}), the robust scores (\code{score_volume}, \code{score_delay}, \code{score_span},
\code{score_gap}) and the \code{batch} flag.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Laboratories sometimes withhold results and then release a whole backlog at
once. Operationally such a \strong{batch} is a \emph{report date} that is anomalous on
the report axis. This scans each report-date period and flags batches using up
to four robust signals, of which you choose the combination to require:
\itemize{
\item \code{"volume"} — unusually many cases reported that period (robust z of the
report count vs a rolling median/MAD baseline).
\item \code{"delay"} — the reports that period have unusually \strong{long} delays (a cleared
backlog). \strong{This is the signal that distinguishes a batch from an epidemic
peak}: an epidemic peak also inflates the volume, but its cases are still
reported with the normal, short delay distribution, so its \code{delay} score
stays low.
\item \code{"span"} — the period covers an unusually large number of \strong{distinct event
dates}.
\item \code{"gap"} — the period is preceded by a run of unusually \strong{low} reporting (the
"silence then dump" cadence).
}

A report date is flagged when \strong{all} the activated \code{signals} fire (each score
at or above \code{threshold}). Requiring \code{"delay"} (or \code{"span"}) alongside
\code{"volume"} is what keeps epidemic peaks from being flagged.
}
\examples{
data(denguedat)
dengue <- tbl_now(denguedat,
  event_date = "onset_week", report_date = "report_week", verbose = FALSE
)
batches <- detect_report_batches(dengue)
batches[batches$batch, ]

}
\seealso{
\code{\link[=plot_report_batches]{plot_report_batches()}}
}
