% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{nowcast_summary_components}
\alias{nowcast_summary_components}
\alias{cases_per_date}
\alias{delay_summary}
\alias{zero_run_summary}
\alias{prop_censored}
\alias{prop_revision_type}
\alias{prop_strata}
\alias{prop_covariate_levels}
\alias{date_ranges}
\alias{triangle_occupancy}
\alias{cumulative_growth}
\title{Individual blocks of a \code{tbl_now} summary}
\usage{
cases_per_date(
  x,
  axis = c("event", "report", "revision"),
  by_strata = NULL,
  strata = NULL
)

delay_summary(
  x,
  delay = c("event_to_report", "event_to_revision", "report_to_revision"),
  by_strata = NULL,
  strata = NULL
)

zero_run_summary(
  x,
  axis = c("event", "report", "revision"),
  by_strata = NULL,
  strata = NULL
)

prop_censored(x, by_strata = NULL, strata = NULL)

prop_revision_type(x, by_strata = NULL, strata = NULL)

prop_strata(x, strata = NULL)

prop_covariate_levels(x, by_strata = NULL, strata = NULL)

date_ranges(x, by_strata = NULL, strata = NULL)

triangle_occupancy(x, by_strata = NULL, strata = NULL)

cumulative_growth(x, k = 7, by_strata = NULL, strata = NULL)
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{axis}{Which time axis to describe: \code{"event"}, \code{"report"} or
\code{"revision"}.}

\item{by_strata}{Logical. Add one set of rows per stratum on top of the
pooled (\code{"all"}) rows. Defaults to \code{TRUE} when the object has strata.}

\item{strata}{Character vector of columns to stratify by. Defaults to
\code{get_strata(x)}.}

\item{delay}{Which delay to describe: \code{"event_to_report"} (the reporting
delay), \code{"event_to_revision"} (the same span measured to the
revision, so the two are comparable) or \code{"report_to_revision"}
(the laboratory's turnaround, the \code{.revision_delay} column).}

\item{k}{Number of delays for the growth ratios.}
}
\value{
A tibble in the schema documented in \link{tbl_now_summary}: one row per
quantity and stratum, with \code{component}, \code{quantity} and \code{stratum} identifying
the row and the remaining columns holding whichever statistics apply.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

\link[=tbl_now_summary]{summary()} answers a dozen questions about a \code{tbl_now} at
once. When you only want one of them -- for a report, a dashboard, or a check
inside a script -- call that block directly instead of computing the rest and
filtering it away.

Every one of these returns the same schema as \code{summary()} itself, so they can
be stacked with \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}}, compared across datasets, or used alone.
\itemize{
\item \code{cases_per_date()} -- case counts per date on one axis.
\item \code{delay_summary()} -- the case-weighted delay distribution.
\item \code{zero_run_summary()} -- lengths of the runs of consecutive zero dates.
\item \code{prop_censored()} -- proportion of cases flagged censored.
\item \code{prop_revision_type()} -- proportion of cases per revision outcome.
\item \code{prop_strata()} -- proportion of cases per stratum.
\item \code{prop_covariate_levels()} -- proportion of cases per level of each
categorical covariate.
\item \code{date_ranges()} -- totals, date ranges and \code{now}.
\item \code{triangle_occupancy()} -- how full the reporting triangle is, and how
stale the object is.
\item \code{cumulative_growth()} -- ratio of one delay's running total to the
previous one's.
}
}
\examples{
data(denguedat)
ndata <- tbl_now(denguedat,
  event_date = "onset_week",
  report_date = "report_week",
  strata = "gender",
  verbose = FALSE
)

# How many cases per week of onset, and how long they took to be reported.
cases_per_date(ndata, axis = "event")
delay_summary(ndata)

# How sparse the series is.
zero_run_summary(ndata, axis = "event")

# What the data is made of, and how far it reaches.
prop_strata(ndata)
prop_censored(ndata)
date_ranges(ndata)
triangle_occupancy(ndata)

# How fast the running total is still growing. This is a distribution over
# event dates, so it fills `mean`/`q50` rather than the scalar `value`.
cumulative_growth(ndata, k = 3)

# Every block shares one schema, so they stack.
dplyr::bind_rows(
  date_ranges(ndata),
  delay_summary(ndata)
)

}
\seealso{
\link[=tbl_now_summary]{summary()}, which stacks all of these into one table and
documents the schema; \code{\link[=diagnose]{diagnose()}} for what is \emph{wrong} with the data rather
than what is in it; \link[=autoplot.tbl_now]{autoplot()} for the same information as
pictures. The
\href{https://rodrigozepeda.github.io/tbl.now/articles/diagnosing-a-tbl-now.html}{\emph{Diagnosing a tbl_now} article}
walks through them in order.
}
