% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{nowcast_summary_components}
\alias{nowcast_summary_components}
\alias{cases_per_date}
\alias{delay_summary}
\alias{zero_run_summary}
\alias{prop_censored}
\alias{prop_confirmation_type}
\alias{prop_strata}
\alias{prop_covariate_levels}
\alias{case_autocorrelation}
\alias{date_ranges}
\alias{triangle_occupancy}
\alias{reporting_completeness}
\alias{cumulative_growth}
\title{Individual blocks of a \code{tbl_now} summary}
\usage{
cases_per_date(
  x,
  axis = c("event", "report", "confirmation"),
  by_strata = NULL,
  strata = NULL
)

delay_summary(
  x,
  delay = c("event_to_report", "event_to_confirmation", "report_to_confirmation"),
  by_strata = NULL,
  strata = NULL
)

zero_run_summary(
  x,
  axis = c("event", "report", "confirmation"),
  by_strata = NULL,
  strata = NULL
)

prop_censored(x, by_strata = NULL, strata = NULL)

prop_confirmation_type(x, by_strata = NULL, strata = NULL)

prop_strata(x, strata = NULL)

prop_covariate_levels(x, by_strata = NULL, strata = NULL)

case_autocorrelation(
  x,
  lags = 1,
  axis = c("event", "report", "confirmation"),
  by_strata = NULL,
  strata = NULL
)

date_ranges(x, by_strata = NULL, strata = NULL)

triangle_occupancy(x, by_strata = NULL, strata = NULL)

reporting_completeness(
  x,
  delays = NULL,
  mature_only = TRUE,
  by_strata = NULL,
  strata = NULL
)

cumulative_growth(x, k = 7, by_strata = NULL, strata = NULL)
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{axis}{Which time axis to describe: \code{"event"}, \code{"report"} or
\code{"confirmation"}.}

\item{by_strata}{Logical. Add one set of rows per stratum on top of the
pooled (\code{"all"}) rows. Defaults to \code{TRUE} when the object has strata.}

\item{strata}{Character vector of columns to stratify by. Defaults to
\code{get_strata(x)}.}

\item{delay}{Which delay to describe: \code{"event_to_report"} (the reporting
delay), \code{"event_to_confirmation"} (the same span measured to the
confirmation, so the two are comparable) or \code{"report_to_confirmation"}
(the laboratory's turnaround, the \code{.confirmation_delay} column).}

\item{lags}{Integer vector of lags.}

\item{delays}{Integer vector of delays to report completeness at. Defaults
to every observed delay.}

\item{mature_only}{Logical. Drop event dates too recent to have been fully
reported. The cutoff is \code{now} minus the 95th percentile of the delay
distribution -- the same rule \code{\link[=autoplot.tbl_now]{autoplot.tbl_now()}} uses.}

\item{k}{Number of delays for the growth ratios.}
}
\value{
A tibble in the schema documented in \link{tbl_now_summary}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Each function returns one block of \code{\link[=summary.tbl_now]{summary.tbl_now()}}, in the same schema,
so they can be stacked with \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}} or used on their own.
\itemize{
\item \code{cases_per_date()} -- case counts per date on one axis.
\item \code{delay_summary()} -- the case-weighted delay distribution.
\item \code{zero_run_summary()} -- lengths of the runs of consecutive zero dates.
\item \code{prop_censored()} -- proportion of cases flagged censored.
\item \code{prop_confirmation_type()} -- proportion of cases per confirmation outcome.
\item \code{prop_strata()} -- proportion of cases per stratum.
\item \code{prop_covariate_levels()} -- proportion of cases per level of each
categorical covariate.
\item \code{case_autocorrelation()} -- lagged autocorrelation of the case series.
\item \code{date_ranges()} -- totals, date ranges and \code{now}.
\item \code{triangle_occupancy()} -- how full the reporting triangle is, and how
stale the object is.
\item \code{reporting_completeness()} -- share of each event date's eventual total
that had arrived by delay \code{d}.
\item \code{cumulative_growth()} -- ratio of one delay's running total to the
previous one's.
}
}
\examples{
data(denguedat)
ndata <- tbl_now(denguedat,
  event_date = "onset_week",
  report_date = "report_week",
  strata = "gender",
  verbose = FALSE
)

cases_per_date(ndata, axis = "event")
delay_summary(ndata)
zero_run_summary(ndata, axis = "event")
prop_strata(ndata)
case_autocorrelation(ndata, lag = 1)
date_ranges(ndata)
triangle_occupancy(ndata)
reporting_completeness(ndata, delays = 0:3)

}
\seealso{
\link{tbl_now_summary}, which stacks all of these.
}
