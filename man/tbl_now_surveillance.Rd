% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/converters.R
\name{tbl_now_surveillance}
\alias{tbl_now_surveillance}
\alias{tbl_now_to_surveillance}
\title{Convert a \code{tbl_now} into the line list \pkg{surveillance} nowcasts from}
\usage{
tbl_now_to_surveillance(
  x,
  ...,
  event_col = "dHospital",
  report_col = "dReport",
  format = c("linelist", "sts"),
  aggregate_by = NULL,
  verbose = TRUE
)
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{...}{Forwarded to \code{\link[surveillance:linelist2sts]{surveillance::linelist2sts()}} when
\code{format = "sts"}; ignored otherwise.}

\item{event_col, report_col}{Names to give the event and report date columns
in the result. Default to \pkg{surveillance}'s own \code{"dHospital"} and
\code{"dReport"}, so \code{\link[surveillance:nowcast]{surveillance::nowcast()}} finds them without further
arguments.}

\item{format}{\code{"linelist"} (default) for the data frame
\code{\link[surveillance:nowcast]{surveillance::nowcast()}} expects, or \code{"sts"} for an
\link[surveillance:sts]{surveillance::sts} object of the observed curve.}

\item{aggregate_by}{Aggregation interval for \code{format = "sts"}, e.g.
\code{"1 week"}. \code{NULL} (default) derives it from the object's event units.}

\item{verbose}{Logical. Print the choices that were made.}
}
\value{
A \code{data.frame} line list (\code{format = "linelist"}) or an
\link[surveillance:sts]{surveillance::sts} object (\code{format = "sts"}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{\link[surveillance:nowcast]{surveillance::nowcast()}} works from an individual-level line list with one
column holding the event date and another the report date, named by its
\code{dEventCol} / \code{dReportCol} arguments. \code{tbl_now_to_surveillance()} produces
exactly that data frame, renaming the two dates to \pkg{surveillance}'s own
defaults so the result can be passed straight through.

With \code{format = "sts"} it instead returns the observed epidemic curve as an
\link[surveillance:sts]{surveillance::sts} object via \code{\link[surveillance:linelist2sts]{surveillance::linelist2sts()}}, which is what
\pkg{surveillance}'s plotting and outbreak-detection verbs consume.

\code{now} and the delay unit are \emph{not} baked into the result: pass them from the
object with \code{\link[=get_now]{get_now()}} and \code{\link[=get_event_units]{get_event_units()}}, as in the example below.
}
\examples{
\dontshow{if (requireNamespace("surveillance", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(denguedat)
nowobj <- tbl_now(denguedat,
  event_date = "onset_week", report_date = "report_week", verbose = FALSE
)
sur <- tbl_now_to_surveillance(nowobj, verbose = FALSE)
head(sur)

# `now` and the aggregation unit come from the object itself:
get_now(nowobj)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=tbl_now_to_nowcaster]{tbl_now_to_nowcaster()}}, \code{\link[=tbl_now_to_epinowcast]{tbl_now_to_epinowcast()}}
}
