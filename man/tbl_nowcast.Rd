% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_class.R
\name{tbl_nowcast}
\alias{tbl_nowcast}
\title{A nowcast produced by \code{\link[=run_nowcast]{run_nowcast()}}}
\usage{
tbl_nowcast(
  predictions = dplyr::tibble(),
  draws = NULL,
  method = NA_character_,
  fit = NULL,
  now = NULL,
  event_date = "event_date",
  strata = character(0),
  data = NULL,
  call = NULL,
  metadata = list()
)
}
\arguments{
\item{predictions}{A \code{tibble} of quantile predictions. One row per
(event date, stratum, quantile level) with the columns
\verb{<event_date>}, the strata columns, \code{.quantile_level} and \code{.value}.}

\item{draws}{Either \code{NULL} (backends that only return quantiles) or a
\code{tibble} with one row per (event date, stratum, draw) and the columns
\verb{<event_date>}, the strata columns, \code{.draw} and \code{.value}.}

\item{method}{The name of the method that produced the nowcast.}

\item{fit}{The untouched object returned by the backend.}

\item{now}{The \code{now} of the nowcast.}

\item{event_date}{Name of the event-date column in \code{predictions}/\code{draws}.}

\item{strata}{Character vector with the names of the strata columns
(\code{character(0)} when the nowcast is not stratified).}

\item{data}{The \code{tbl_now} the nowcast was produced from.}

\item{call}{The matched call of \code{\link[=run_nowcast]{run_nowcast()}}.}

\item{metadata}{A named list with anything else the backend wants to keep.}
}
\value{
A \code{tbl_nowcast} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

An S7 object holding a nowcast in a package-agnostic shape. It is what
\code{\link[=run_nowcast]{run_nowcast()}} returns and what \code{\link[=nowcast_ensemble]{nowcast_ensemble()}} and \code{\link[=score_nowcast]{score_nowcast()}}
consume.
}
\examples{
# Normally built for you by the one-call front door, but the constructor is
# exported because a new backend -- or a test -- needs to build one directly.
predictions <- data.frame(
  onset_week = as.Date("2020-01-05") + c(0, 0, 7, 7),
  .quantile_level = c(0.5, 0.9, 0.5, 0.9),
  .value = c(10, 14, 12, 17)
)
tbl_nowcast(predictions = predictions, method = "toy", event_date = "onset_week")

}
\seealso{
\code{\link[=run_nowcast]{run_nowcast()}}, \code{\link[=nowcast_ensemble]{nowcast_ensemble()}}, \code{\link[=score_nowcast]{score_nowcast()}}
}
