% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/revision.R
\name{revision_delay}
\alias{revision_delay}
\alias{diagnose_revision_delay}
\title{Compare revision delays between confirmed and retracted cases}
\usage{
diagnose_revision_delay(x, by = NULL)
}
\arguments{
\item{x}{A \code{tbl_now} with a revision process.}

\item{by}{Optional stratum column to compare within; \code{NULL} (default) pools.}
}
\value{
\code{diagnose_revision_delay()} returns a one-row-per-comparison \code{tibble} with
\code{stratum}, \code{n_confirmed}, \code{n_retracted}, \code{median_confirmed},
\code{median_retracted}, \code{difference}, \code{statistic} and \code{p.value}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

A negative result often comes back faster than a positive one -- or slower,
if positives are prioritised. Either way the delay from report to resolution
is \strong{not} the same distribution for the two outcomes, and a nowcast that
assumes it is will be wrong about how many pending cases are still to be
confirmed.

\code{diagnose_revision_delay()} compares the two delay distributions;
\code{\link[=plot_delay_distribution]{plot_delay_distribution()}} with \code{axis = "revision"} shows them.
}
\section{The test}{


A two-sided \strong{Wilcoxon rank-sum} test on the revision delays. It is used
rather than a t-test because reporting delays are strongly right-skewed and
frequently have a point mass at zero, so a difference in means is neither
robust nor the quantity of interest -- what matters is whether one outcome
resolves systematically sooner.

A small p-value says the two delay distributions differ. It does \strong{not} say
the difference matters: with tens of thousands of records a one-hour
difference is significant and irrelevant, so read \code{difference} (the gap in
median days) alongside it.

Rows with a missing or negative delay are dropped, and how many is reported
in the \code{dropped} attribute of the result. A negative revision delay means
the record is revised before it was reported, which the timeline forbids.
}

\examples{
cases <- data.frame(
  onset = as.Date("2021-01-04") + rep(0:9, each = 4),
  visit = as.Date("2021-01-05") + rep(0:9, each = 4),
  result = as.Date("2021-01-05") + rep(0:9, each = 4) +
    rep(c(1, 1, 5, 6), times = 10),
  outcome = rep(c("confirmed", "confirmed", "retracted", "retracted"), times = 10)
)
flu <- tbl_now(cases,
  event_date = onset, report_date = visit,
  revision_date = result, revision_type = outcome,
  data_type = "linelist", verbose = FALSE
)

# Retractions here come back about four days later than revisions, and
# the test says so.
diagnose_revision_delay(flu)

# The same comparison as a picture.
plot_delay_distribution(flu, axis = "revision")

}
\seealso{
\link[=add]{add_revision_date()} to attach a revision process;
\link[=censoring]{censor_revision_delays_above()} for resolutions that
never arrive; \link{revised_cases} for counting the outcomes;
\code{\link[=plot_delay_distribution]{plot_delay_distribution()}} with \code{axis = "revision"} for the picture of the
same comparison; \code{\link[=diagnose_drift]{diagnose_drift()}} for the same question about the
\emph{reporting} delay over time.
The \href{https://rodrigozepeda.github.io/tbl.now/articles/diagnosing-a-tbl-now.html}{\emph{Diagnosing a tbl_now} article}
puts this alongside the other checks.
}
