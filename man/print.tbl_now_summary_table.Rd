% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/summary.R
\name{print.tbl_now_summary_table}
\alias{print.tbl_now_summary_table}
\title{Print a \code{tbl_now} summary}
\usage{
\method{print}{tbl_now_summary_table}(x, ..., n = 10)
}
\arguments{
\item{x}{A summary tibble, from \link[=tbl_now_summary]{summary()} or one of the
\link{nowcast_summary_components}.}

\item{...}{Unused.}

\item{n}{Maximum number of rows to show per component. \code{Inf} shows all of
them.}
}
\value{
\code{x}, invisibly.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Prints the table \link[=tbl_now_summary]{summary()} returned one \strong{component} at a
time, dropping the columns that component does not populate. The full schema
is wide because it has to hold every block's statistics at once; no single
block fills more than a handful of them, and a table that is mostly \code{NA} is
hard to read for a reason that has nothing to do with the data.

The object is an ordinary tibble underneath, so
\code{print(tibble::as_tibble(x))} gives the whole schema back and every \code{dplyr}
verb still works on it.
}
\examples{
data(denguedat)
ndata <- tbl_now(denguedat,
  event_date = "onset_week", report_date = "report_week",
  strata = "gender", verbose = FALSE
)

summary(ndata)

# One block on its own prints the same way.
delay_summary(ndata)

# Still a tibble.
print(tibble::as_tibble(summary(ndata)))

}
\seealso{
\link[=tbl_now_summary]{summary()}, \link{nowcast_summary_components}
}
