% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_engine.R
\name{nowcast_engines}
\alias{nowcast_engines}
\alias{engine_diseasenowcasting}
\alias{engine_baselinenowcast}
\alias{engine_epinowcast}
\alias{engine_nobbs}
\alias{engine_surveillance}
\alias{engine_epinow2}
\title{Engines for the built-in nowcasting packages}
\usage{
engine_diseasenowcasting(
  ...,
  model = NULL,
  type = NULL,
  n_draws = NULL,
  min_date = NULL,
  quantile_levels = nowcast_quantile_levels(),
  label = NULL
)

engine_baselinenowcast(
  ...,
  draws = 1000,
  delays_unit = NULL,
  min_date = NULL,
  quantile_levels = nowcast_quantile_levels(),
  label = NULL
)

engine_epinowcast(
  ...,
  preprocess_args = list(),
  expectation = NULL,
  reference = NULL,
  report = NULL,
  fit = NULL,
  min_date = NULL,
  quantile_levels = nowcast_quantile_levels(),
  label = NULL
)

engine_nobbs(
  ...,
  max_D = NULL,
  moving_window = NULL,
  specs = NULL,
  min_date = NULL,
  quantile_levels = nowcast_quantile_levels(),
  label = NULL
)

engine_surveillance(
  ...,
  D = NULL,
  when = NULL,
  fit_method = NULL,
  control = NULL,
  min_date = NULL,
  quantile_levels = nowcast_quantile_levels(),
  label = NULL
)

engine_epinow2(
  ...,
  generation_time = NULL,
  delays = NULL,
  truncation = NULL,
  rt = NULL,
  stan = NULL,
  convert_args = list(),
  min_date = NULL,
  quantile_levels = nowcast_quantile_levels(),
  label = NULL
)
}
\arguments{
\item{...}{Further arguments for the modelling function, passed through
untouched. In \code{engine()} this is \emph{every} argument; in the \verb{engine_*()}
functions it is whatever their named arguments do not already cover.}

\item{model, type, n_draws}{(\code{engine_diseasenowcasting()}) Arguments of
\code{\link[diseasenowcasting:nowcast]{diseasenowcasting::nowcast()}}. \code{model} is where the epidemic and
confirmation processes are chosen, e.g.
\code{diseasenowcasting::model(epidemic = diseasenowcasting::ar1_epidemic())}.
On \code{count-cumulative} data that revises downwards you also want a
\code{confirmation} process, or the negative increments have nowhere to go.}

\item{min_date}{How much history to fit on. One of
\itemize{
\item \code{NULL} (default) -- the whole series;
\item a \strong{\code{Date}} -- keep event dates on or after it;
\item a \strong{single number} -- keep the last \emph{n} periods before the object's
\code{now}, counted in the object's event units.
}

The number is usually what you want in a \code{\link[=nowcast_backtest]{nowcast_backtest()}}: \code{now} moves
between fits, and a fixed calendar date would make the fitted window grow as
the backtest walks forward, so the last fit would be trained on more data
than the first. Trimming is per engine on purpose -- \code{baselinenowcast} and
\code{diseasenowcasting} take a long series in their stride, while \code{epinowcast}
scales with the number of reference dates and is best given a window.}

\item{quantile_levels}{Numeric vector of probabilities to report the nowcast
at. Defaults to \code{\link[=nowcast_quantile_levels]{nowcast_quantile_levels()}}.

It lives on the engine because for some backends it is a \strong{fit-time model
argument}, not a way of summarising afterwards. \pkg{NobBS} computes
exactly the quantiles it is handed in \code{specs$quantiles} and keeps no draws,
so a level it was never asked for cannot be recovered, and
\pkg{surveillance} reports a fixed set and warns rather than interpolating.
The draw-keeping backends -- \code{baselinenowcast}, \code{diseasenowcasting},
\code{epinowcast} and \code{EpiNow2} -- answer any level after the fact.}

\item{label}{Name for this engine in a \code{\link[=nowcast_backtest]{nowcast_backtest()}} and in the
ensemble weights derived from one. Defaults to the method name. Give one
when the same package appears twice with different settings, which is the
whole reason two \code{diseasenowcasting} models can be weighted separately.}

\item{draws, delays_unit}{(\code{engine_baselinenowcast()}) Number of nowcast
samples, and the unit of the reporting triangle's delay axis (inferred from
the object's units when \code{NULL}).}

\item{preprocess_args, expectation, reference, report, fit}{(\code{engine_epinowcast()}) \code{preprocess_args} is a list for
\code{\link[=tbl_now_to_epinowcast]{tbl_now_to_epinowcast()}}, e.g. \code{list(max_delay = 30)}; the other four are
\code{epinowcast::epinowcast()}'s module arguments. \strong{\code{epinowcast} is unseeded
unless you say so}: \code{enw_fit_opts()} passes \code{...} to the sampler, so
\code{fit = epinowcast::enw_fit_opts(seed = 1)} is what makes a fit reproducible.}

\item{max_D, moving_window, specs}{(\code{engine_nobbs()}) Arguments of
\code{NobBS::NobBS()} / \code{NobBS::NobBS.strat()}. \code{moving_window} counts \strong{event
periods and must not exceed the history you hand it} -- ask for more and
NobBS pads its grid backwards and returns zero for every date, with no
error. \code{specs$quantiles} is filled from \code{quantile_levels} unless you set it.}

\item{D, when, fit_method, control}{(\code{engine_surveillance()}) Arguments of
\code{\link[surveillance:nowcast]{surveillance::nowcast()}}. \code{fit_method} is that function's own \code{method}
argument, renamed so it cannot collide with the engine's method. \code{when}
defaults to \code{get_surveillance_when(x, length = D + 1)} and \code{control$dRange}
to \code{\link[=get_surveillance_range]{get_surveillance_range()}} -- both built from the whole object, so every
stratum is fitted on the same time axis.}

\item{generation_time, delays, truncation, rt, stan, convert_args}{(\code{engine_epinow2()}) Arguments of \code{EpiNow2::estimate_infections()} /
\code{EpiNow2::regional_epinow()}, plus \code{convert_args} for
\code{\link[=tbl_now_to_EpiNow2]{tbl_now_to_EpiNow2()}}. \strong{Read this before trusting the output}:
EpiNow2 defaults to \code{delays = delay_opts()}, which is \code{Fixed(0)} -- no
reporting delay at all -- and a one-day generation time. Those defaults
describe a process with nothing to nowcast, so supply the epidemiology
yourself.}
}
\value{
A \code{nowcast_engine}, as \code{\link[=engine]{engine()}} returns.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

One constructor per supported modelling package. Each is \code{\link[=engine]{engine()}} with the
arguments of that package's own entry point spelled out, so the ones that
matter are visible in the signature and a typo is an error rather than a
silently ignored extra.

The \href{https://rodrigozepeda.github.io/tbl.now/articles/nowcasting-models.html}{\emph{One dataset, many nowcasts} article}
documents each package's own API; this page
is about driving it through \code{\link[=run_nowcast]{run_nowcast()}}.
}
\examples{
engine_baselinenowcast(draws = 500)
engine_nobbs(max_D = 10, moving_window = 64)

# Fit epinowcast on the last 180 periods only; it scales with the number of
# reference dates, while the two engines below take the whole series.
engine_epinowcast(preprocess_args = list(max_delay = 30), min_date = 180)
engine_baselinenowcast()
engine_diseasenowcasting()

}
\seealso{
\code{\link[=engine]{engine()}}, \code{\link[=run_nowcast]{run_nowcast()}}, \code{\link[=nowcast_backtest]{nowcast_backtest()}}
}
