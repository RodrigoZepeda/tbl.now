% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_methods.R, R/run_nowcast.R
\name{nowcast_tidy.diseasenowcasting}
\alias{nowcast_tidy.diseasenowcasting}
\alias{nowcast_tidy.baselinenowcast}
\alias{nowcast_tidy.epinowcast}
\alias{nowcast_tidy.NobBS}
\alias{nowcast_tidy.surveillance}
\alias{nowcast_tidy.EpiNow2}
\alias{nowcast_tidy}
\alias{nowcast_tidy.default}
\title{Standardise a fitted nowcast}
\usage{
\method{nowcast_tidy}{diseasenowcasting}(engine, fit, x, ..., quantile_levels)

\method{nowcast_tidy}{baselinenowcast}(engine, fit, x, ..., quantile_levels)

\method{nowcast_tidy}{epinowcast}(engine, fit, x, ..., quantile_levels)

\method{nowcast_tidy}{NobBS}(engine, fit, x, ..., quantile_levels)

\method{nowcast_tidy}{surveillance}(engine, fit, x, ..., quantile_levels)

\method{nowcast_tidy}{EpiNow2}(engine, fit, x, ..., quantile_levels)

nowcast_tidy(engine, fit, x, ..., quantile_levels)

\method{nowcast_tidy}{default}(engine, fit, x, ..., quantile_levels)
}
\arguments{
\item{engine}{An \code{\link[=engine]{engine()}} object; see \code{\link[=nowcast_fit]{nowcast_fit()}}.}

\item{fit}{The object returned by \code{\link[=nowcast_fit]{nowcast_fit()}}.}

\item{x}{The \code{tbl_now} the nowcast was produced from.}

\item{...}{Not forwarded by \code{\link[=run_nowcast]{run_nowcast()}}, which passes the user's \code{...} to
\code{\link[=nowcast_fit]{nowcast_fit()}} only. Anything the tidying step needs -- a number of draws,
a tuning parameter, a lookup table -- must therefore travel inside the
object \code{nowcast_fit()} returned.}

\item{quantile_levels}{Numeric vector of probabilities the predictions should
be summarised at.}
}
\value{
A list with two elements:

\describe{
\item{predictions}{A data frame with one row per (event date, stratum,
quantile level) and the columns \verb{<event_date>}, the strata columns,
\code{.quantile_level} and \code{.value}.}
\item{draws}{Either \code{NULL}, or a data frame with one row per (event date,
stratum, draw) and the columns \verb{<event_date>}, the strata columns,
\code{.draw} and \code{.value}.}
}

When \code{draws} is supplied and \code{predictions} is \code{NULL}, \code{\link[=run_nowcast]{run_nowcast()}}
derives the quantiles from the draws for you.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The second extension point of the nowcasting framework (see \code{\link[=nowcast_fit]{nowcast_fit()}}).
It receives the object the modelling package returned and must express its
predictions in the tidy format tbl.now uses everywhere else.
}
\examples{
# See `?nowcast_fit` for a complete two-method backend.
methods(nowcast_tidy)

}
\seealso{
\code{\link[=nowcast_fit]{nowcast_fit()}}, \code{\link[=run_nowcast]{run_nowcast()}}
}
