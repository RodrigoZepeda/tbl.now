% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostic_plot.R
\name{diagnostic_plot}
\alias{diagnostic_plot}
\title{Diagnostic plots of the reporting process}
\usage{
diagnostic_plot(
  x,
  panels = "all",
  by = c("report", "event"),
  max_delay = NULL,
  ...,
  plotly = FALSE,
  axis = c("report", "validation"),
  palette = .tbl_now_palette()
)
}
\arguments{
\item{x}{A \code{\link[=tbl_now]{tbl_now()}} object.}

\item{panels}{Which panels, \code{"all"} (default) or any subset of \code{"reporting"},
\code{"triangle"}, \code{"profiles"}, \code{"delay_drift"} and \code{"transport"}.}

\item{by}{For the \code{"profiles"} panel, one mark per \code{"report"} date (default) or
per \code{"event"} date.}

\item{max_delay}{Largest delay on the delay-based panels. \code{NULL} (default) caps
at the delay covering 99\% of reported mass.}

\item{...}{Batch controls (\code{lookback}, \code{period}, \code{alpha}) routed to the
\code{"transport"} panel.}

\item{plotly}{If \code{TRUE}, return an interactive \pkg{plotly} widget (the panels
stacked) instead of a static \pkg{patchwork}. Default \code{FALSE}.}

\item{axis}{Which time axis the delay is measured to: \code{"report"} (default)
or \code{"validation"}. Both are measured \emph{from the event}, so the two are
directly comparable -- run each in turn and the gap between them is the
time the laboratory adds. (This is not the same quantity as the
\code{.validation_delay} column, which is the laboratory's own turnaround,
measured from the report.) Needs a validation process (see
\code{\link[=add_validation_date]{add_validation_date()}}); cases still \code{"pending"} are left out.}

\item{palette}{A named colour palette. Defaults to the package palette.}
}
\value{
A \pkg{patchwork} object, or a single plot when one panel is selected
(or a \pkg{plotly} widget when \code{plotly = TRUE}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Lays out a gallery of complementary views of a \code{tbl_now}'s reporting process,
all aimed at spotting reporting artefacts -- especially \emph{batch reporting}. Each
view is also available on its own (see \strong{See also}); \code{diagnostic_plot()} picks
the ones named in \code{panels} and combines them with \pkg{patchwork}. Selecting a
single panel returns it as a plain plot. Every view is facetted by stratum when
the \code{tbl_now} declares strata.
}
\examples{
\dontshow{if (requireNamespace("patchwork", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(denguedat)
dn <- tbl_now(denguedat, onset_week, report_week, verbose = FALSE)
diagnostic_plot(dn, panels = c("triangle", "transport"))
\dontshow{\}) # examplesIf}
}
\seealso{
Every panel is also a function of its own:
\link[=plot_epidemic_process]{plot_reporting_process()} and
\code{\link[=plot_epidemic_process]{plot_epidemic_process()}} (when reports arrived, versus when cases happened),
\code{\link[=plot_reporting_triangle]{plot_reporting_triangle()}} (the full event-by-delay grid),
\code{\link[=plot_delay_profiles]{plot_delay_profiles()}} (each date's delay curve),
\code{\link[=plot_delay_drift]{plot_delay_drift()}} (whether delays are getting longer),
\code{\link[=plot_transport_discriminant]{plot_transport_discriminant()}}, \code{\link[=plot_scalogram]{plot_scalogram()}}.
}
