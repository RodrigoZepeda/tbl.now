% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose.R
\name{diagnose}
\alias{diagnose}
\alias{diagnose.default}
\alias{diagnose.tbl_now}
\title{Diagnose a \code{tbl_now}}
\usage{
diagnose(x, ...)

\method{diagnose}{default}(x, ...)

\method{diagnose}{tbl_now}(
  x,
  ...,
  checks = NULL,
  by_strata = NULL,
  strata = NULL,
  warn_non_uniqueness = TRUE
)
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{...}{Unused, for extensibility.}

\item{checks}{Character vector of checks to run, a subset of
\code{c("declarations", "ordering", "missing", "duplicates", "units", "negatives", "now", "truncation", "strata", "signposts")}. Defaults to all
of them.}

\item{by_strata}{Logical. Add one set of rows per stratum, for the checks
that are naturally per-stratum (missingness, negative increments,
right-truncation, the gap to \code{now}). Defaults to \code{TRUE} when the object has
strata. The checks that are statements about the object as a whole
(declarations, units, duplicates, ordering) are always reported once, with
\code{stratum = "all"}.}

\item{strata}{Character vector of columns to stratify by. Defaults to
\code{get_strata(x)}.}

\item{warn_non_uniqueness}{Logical. Run the duplicate-row check. Defaults to
\code{TRUE} here, unlike \code{\link[=validate_tbl_now]{validate_tbl_now()}}, where it defaults to \code{FALSE}
because it runs on every \code{dplyr} verb.}
}
\value{
A tibble with the columns described above, sorted worst first.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{diagnose()} is a structural health check. It looks for the things that make
a nowcast wrong before any model is fitted -- dates out of order, missing
values, repeated rows, units that disagree, data after \code{now}, event dates too
recent to be complete -- and returns them as a tibble of findings, sorted
worst first.

It is \strong{deterministic and runs no statistical test.} Whether the reporting
delay drifts, and whether reports arrive in batches, are questions about a
\emph{distribution}, not about the object's structure; \code{diagnose()} emits a
\code{"not_run"} signpost naming the function to call instead
(\code{\link[=diagnose_drift]{diagnose_drift()}}, \code{\link[=diagnose_batches]{diagnose_batches()}}) rather than quietly running a test
whose method, window and multiplicity correction you did not choose.

Every block is also available on its own -- see
\link{nowcast_diagnose_components} -- and \code{diagnose()} is exactly the
\code{\link[dplyr:bind_rows]{dplyr::bind_rows()}} of those pieces.
}
\section{The columns}{


Every function in this family returns the same schema, so results can be
stacked with \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}} and filtered with \code{\link[dplyr:filter]{dplyr::filter()}}.

\describe{
\item{\code{check}}{Which block the row belongs to: \code{"declarations"},
\code{"ordering"}, \code{"missing"}, \code{"duplicates"}, \code{"units"}, \code{"negatives"},
\code{"now"}, \code{"truncation"}, \code{"strata"} or \code{"signposts"}.}
\item{\code{scope}}{What the row is about: a column name, a time axis, a pair of
axes, or \code{"all"}.}
\item{\code{stratum}}{Which subset of the data the row describes: \code{"all"} for
the pooled rows, or the stratum label otherwise.}
\item{\code{status}}{An \strong{ordered factor}, worst first, so the tibble sorts
itself: \code{error} > \code{warning} > \code{note} > \code{ok} > \code{not_run} > \code{skipped}. See
the section below.}
\item{\code{n_affected}}{How many rows (or cases, or dates) the finding is
about.}
\item{\code{n_total}}{How many were considered.}
\item{\code{prop}}{\code{n_affected / n_total}.}
\item{\code{message}}{One human sentence, already formatted.}
\item{\code{hint}}{What to do about it, or \code{NA}.}
\item{\code{rows}}{A list-column of offending row indices, so
\code{x[result$rows[[1]], ]} goes straight to the bad rows. Empty when the
finding is not about particular rows, or when it was computed on a
de-accumulated view whose rows are not the object's own.}
}
}

\section{What the statuses mean}{


\describe{
\item{\code{error}}{\code{\link[=validate_tbl_now]{validate_tbl_now()}} aborts on this. The object is not a
usable \code{tbl_now}.}
\item{\code{warning}}{\code{\link[=validate_tbl_now]{validate_tbl_now()}} warns about this.}
\item{\code{note}}{A \code{diagnose()}-only observation worth your attention. It is
deliberately never promoted to a warning: \code{validate_tbl_now()} runs on
every \code{dplyr} verb, and a new warning there would turn a quiet
construction into a noisy one for data that has always been accepted.}
\item{\code{ok}}{The check ran and found nothing.}
\item{\code{not_run}}{A signpost: this question needs a statistical test, and
\code{message} names the call that answers it.}
\item{\code{skipped}}{Could not be assessed -- no validation process, the
wrong data type, or an optional package that is not installed.}
}
}

\examples{
data(denguedat)
ndata <- tbl_now(denguedat,
  event_date = "onset_week",
  report_date = "report_week",
  strata = "gender",
  verbose = FALSE
)

# Everything, worst first
diagnose(ndata)

# Only what needs acting on
diagnose(ndata) |> dplyr::filter(status <= "note")

# One block on its own
diagnose(ndata, checks = "units")

## `diagnose()` never stops your pipeline -- it hands back a table for you to
## read. Use validate_tbl_now() when you want a broken object to be an error.
nrow(diagnose(ndata))

}
\seealso{
\link{nowcast_diagnose_components} for the individual blocks;
\link[=tbl_now_summary]{summary()} for the descriptive counterpart -- what is in the
data rather than what is wrong with it;
\code{\link[=validate_tbl_now]{validate_tbl_now()}} for the same findings raised as errors and warnings;
\code{\link[=diagnostic_plot]{diagnostic_plot()}} for the picture version. The
\href{https://rodrigozepeda.github.io/tbl.now/articles/diagnosing-a-tbl-now.html}{\emph{Diagnosing a tbl_now} article}
goes through the findings one at a time.
}
