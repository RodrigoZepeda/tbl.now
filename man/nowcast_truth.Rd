% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_score.R
\name{nowcast_truth}
\alias{nowcast_truth}
\title{The final observed counts of a \code{tbl_now}}
\usage{
nowcast_truth(x, strata = get_strata(x))
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{strata}{Character vector of strata columns to keep. Defaults to the
object's declared strata.}
}
\value{
A \code{tibble} with the event-date column, the strata columns and
\code{.observed}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The "truth" a nowcast is scored against: everything that was eventually
reported for each event date, aggregated over the columns that are neither
the event date nor a stratum.

Pass the \emph{full} object here, not the truncated snapshot the nowcast was fitted
on: the point of the comparison is that the truth contains reports the model
had not seen.
}
\examples{
data(denguedat)
recent <- subset(denguedat, onset_week >= as.Date("2010-06-01"))
dengue <- tbl_now(recent,
  event_date = onset_week, report_date = report_week, verbose = FALSE
)
nowcast_truth(dengue)

}
\seealso{
\code{\link[=score_nowcast]{score_nowcast()}}, \code{\link[=nowcast_backtest]{nowcast_backtest()}}
}
