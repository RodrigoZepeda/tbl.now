% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_screen.R
\name{batch_screen}
\alias{batch_screen}
\title{Screen the report axis for batched reporting}
\usage{
batch_screen(
  data,
  lookback = 3L,
  baseline_window = NULL,
  baseline_method = c("repeated_median", "running_median"),
  period = NULL,
  null_model = c("auto", "poisson", "robust"),
  alpha = 0.05
)
}
\arguments{
\item{data}{A \code{\link[=tbl_now]{tbl_now()}} object of any \code{data_type}.}

\item{lookback}{Integer \code{k}: how many report dates before \code{r} the window
reaches back.  Should comfortably cover the longest plausible stall.
Default \code{3}.}

\item{baseline_window}{Odd integer width of the running median used to
estimate the baseline.  Must satisfy \code{baseline_window >= 2 * lookback + 3}
so that a clean date is never outvoted by a batch episode.  Defaults to the
smallest admissible odd value (adjusted upward to a multiple of \code{period}
plus one when \code{period} is supplied).}

\item{baseline_method}{How the baseline is smoothed.  \code{"repeated_median"}
(default) fits a robust local \emph{line} (Siegel's repeated median), which keeps
the 50\% breakdown that makes a median immune to the batch episode while
remaining unbiased when the report series trends -- essential on an epidemic
curve, where a local \emph{constant} fit would call every rising date a surge.
\code{"running_median"} fits a local constant; use it for a flat series.}

\item{period}{Optional integer cycle length of a \emph{scheduled} reporting
pattern (e.g. \code{7} for a weekly cycle on daily data).  \code{NULL} (default)
disables the calendar correction.}

\item{null_model}{\code{"auto"} (default) uses the exact Poisson/Binomial null for
non-negative counts and the robust normal approximation for signed
(count-cumulative) increments.  \code{"poisson"} and \code{"robust"} force the choice.}

\item{alpha}{Significance level for the Benjamini-Hochberg flag and for the
\code{classification} column.  Default \code{0.05}.}
}
\value{
A tibble of class \code{batch_screen}, one row per (report date, stratum),
with the columns described under \strong{Details}.  Has a \code{print()} method that
summarises the flagged dates.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Detects \strong{batches}: report dates at which a stalled reporting system releases
a backlog.  A batch \emph{moves} reports along the report axis without creating
them, so it shows up as a spike preceded by a deficit, while the total over a
window spanning both is unchanged.  \code{batch_screen()} is completely
\strong{model-free} -- it needs only a \code{\link[=tbl_now]{tbl_now()}}, not a fitted model -- which
makes it the right tool for exploratory data analysis before any nowcasting
model is chosen.
}
\section{The mathematics}{
Index each item by its \strong{event date} \eqn{t} (when it happened) and its
\strong{report date} \eqn{r} (when it was recorded); the reporting delay is
\eqn{d = r - t}.  Let \eqn{R_r} be the number of items reported on date \eqn{r}
and \eqn{\mu_r} its expected value under a stable reporting process.

\strong{A batch is a transport, not a creation.}  When a stalled desk releases a
backlog, it \emph{relabels the report date} of items that already exist; it never
creates or destroys them, and it can only ever move an item \emph{later}.  This has
an exact consequence.  Over a window \eqn{\mathcal{W} = \{r-k,\dots,r\}} that
contains both the lull and the spike, write the window total and its null mean

\deqn{S = \sum_{j\in\mathcal{W}} R_j, \qquad M = \sum_{j\in\mathcal{W}} \mu_j.}

The \strong{transport discriminant} is their difference,

\deqn{\Delta_r(k) = (R_r - \mu_r) - \underbrace{\textstyle\sum_{j<r}(\mu_j - R_j)}_{\text{deficit } W_r(k)} = S - M,}

i.e. simply the window total, centred.  \strong{Theorem.} Under \emph{any} batch
mechanism whose displacements stay inside \eqn{\mathcal{W}} -- deterministic or
random, clearing the backlog in any order -- the count in the window is a
\emph{pathwise} invariant, so \eqn{S \sim \mathrm{Poisson}(M)} exactly, with
\eqn{\mathbb{E}\,\Delta = 0}.  A genuine surge that \emph{creates} items with mean
\eqn{\eta} instead gives \eqn{S \sim \mathrm{Poisson}(M + \eta)}, so
\eqn{\mathbb{E}\,\Delta = \eta}.  Hence \eqn{\Delta} sees only creation and the
deficit \eqn{W} sees only transport, and the pair separates a batch from a
surge:

\tabular{lll}{
\tab \eqn{W \approx 0} \tab \eqn{W \gg 0} \cr
\eqn{\Delta \approx 0} \tab nothing \tab \strong{batch} \cr
\eqn{\Delta \gg 0} \tab \strong{surge} \tab batch and surge \cr
\eqn{\Delta < 0} \tab -- \tab \strong{hold in progress / deletion} \cr
}

\strong{Estimating \eqn{M} without cheating.} The theorem is about the \emph{true} mean.
If \eqn{M} were smoothed from a series containing the very episode under test,
the deficits would drag it down and \eqn{\Delta} would acquire a spurious
positive mean -- the batch would mask itself as a surge.  \code{batch_screen()}
therefore refits the baseline for each candidate window from report dates lying
strictly \emph{outside} that window (the model-free analogue of leaving an
observation out).  Because the transport never crosses the window boundary,
\eqn{M} cannot see it, and \eqn{\Delta} is invariant to the batch pathwise.  The
baseline is a robust local line (Siegel's repeated median), which keeps a 50\%
breakdown point against the episode while remaining unbiased under a trend.
}

\section{What is computed}{
For every report date \code{r} and stratum, with a look-back window
\verb{W = \{r - lookback, ..., r\}}:

\describe{
\item{\code{reported}}{\eqn{R_r}, the number of items reported on date \code{r}.  For
\code{"count-cumulative"} data these are the \emph{signed increments} of the
cumulative curve, so \code{reported} may be negative (a net down-revision).}
\item{\code{baseline}}{\eqn{\hat\mu_r}, a robust running-median estimate of how
many reports date \code{r} \emph{should} have carried.}
\item{\code{deficit}}{\eqn{W_r(k) = \sum_{j<r}(\hat\mu_j - R_j)}, the reports
that went missing in the \code{lookback} dates before \code{r}.}
\item{\code{delta}}{\eqn{\Delta_r(k) = S - M}, the window total minus its null
mean.  Blind to any transport confined to the window; sensitive to
creation.}
\item{\code{p_transport}, \code{p_creation}, \code{p_deletion}}{One-sided \eqn{p}-values
for the three directions.}
\item{\code{classification}}{One of \code{"batch"}, \code{"surge"}, \code{"batch_and_surge"},
\code{"hold_or_deletion"}, \code{"none"}.}
\item{\code{batch}}{Logical: \code{p_transport} survives a Benjamini-Hochberg
correction across all (report date, stratum) pairs at level \code{alpha}.}
}
}

\section{The null distribution}{
For non-negative counts (\code{"linelist"} and \code{"count-incidence"} data):
\itemize{
\item \emph{transport} (\strong{exact}): conditionally on the window total \eqn{S}, the
reports allocate across the window's dates multinomially, so the share
landing on the final date is
\eqn{R_r \mid S \sim \mathrm{Binomial}(S, \hat\mu_r / M)}.  A batch pushes
reports to the \emph{last} date, so we test the upper tail.  Conditioning on
\eqn{S} removes the unknown intensity entirely, which is what makes this test
exactly the right size whatever the truth happens to be -- \strong{and what makes it
insensitive to error in the baseline.}
\item \emph{creation} (\strong{dispersion-corrected}): the theory gives
\eqn{S \sim \mathrm{Poisson}(M)} exactly under any within-window batch, but
with \eqn{M} \emph{known}.  Here \eqn{M} is estimated from the same series, so
\eqn{\Delta} carries the baseline's error as well as Poisson noise.  An exact
Poisson tail would then be anti-conservative, so the creation test is widened
by a robustly-estimated quasi-Poisson dispersion.
}

The asymmetry is not an accident: it is the practical face of the conditioning
argument.  The batch verdict is the trustworthy one.

For \code{"count-cumulative"} data the increments are signed, the window total is
a difference of two counting processes rather than a count, and the exact
Poisson/Binomial reference is replaced by a robust normal approximation
(\code{null_model = "robust"}).  The conservation logic is unchanged; only the
reference law degrades.
}

\section{A caveat on the \code{"surge"} verdict}{
The two channels are \emph{not} equally trustworthy without a model.  The
\strong{transport} verdict (\code{"batch"}) compares dates \emph{within} one window and is
insensitive to the overall level -- in the exact Poisson path it literally
conditions on the window total -- so it is stable.  The \strong{creation} verdict
(\code{"surge"}) compares the window total against the baseline, and is therefore
only as good as the baseline.  A robust local-linear baseline is unbiased under
a \emph{linear} trend but not under a sharply \emph{curved} one, so on a steeply growing
epidemic curve \code{"surge"} will fire on ordinary growth.  That is not a bug: mass
really is being created.  It does mean that if you want to detect genuine
surges you should fit a model rather than rely on this screen.
}

\section{Calendar effects}{
A reporting system that is always closed at weekends produces every batch
symptom, every week.  Pass \code{period} (e.g. \code{period = 7} for daily data) and
the baseline is corrected by per-phase medians taken across cycles, which
recovers the schedule exactly provided each phase is hit by \emph{irregular}
batches in fewer than half of its cycles.  An irregular batch is then an
excursion \emph{relative to the schedule}.
}

\examples{
library(tbl.now)
data(denguedat, package = "tbl.now")

dengue_tbl <- tbl_now(
  denguedat,
  event_date  = onset_week,
  report_date = report_week,
  data_type   = "linelist",
  verbose     = FALSE
)

screened <- batch_screen(dengue_tbl, lookback = 2)
head(screened)

}
\seealso{
\code{\link[=batch_shape_test]{batch_shape_test()}} for the complementary test on \emph{which} event
dates a report date drew from, and \code{\link[=simulate_batch]{simulate_batch()}} to inject a known
batch for validation.
}
