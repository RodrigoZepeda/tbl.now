% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/transport_discriminant.R
\name{transport_discriminant}
\alias{transport_discriminant}
\title{The transport discriminant of a reporting series}
\usage{
transport_discriminant(
  x,
  lookback = 7L,
  baseline_window = NULL,
  period = NULL,
  alpha = 0.05,
  axis = c("report", "validation"),
  drop_censored = TRUE
)
}
\arguments{
\item{x}{A \code{\link[=tbl_now]{tbl_now()}} object.}

\item{lookback}{Integer window half-width \code{k} (report-grid steps) over which the
deficit is accumulated. Default \code{7} (a week of daily reporting).}

\item{baseline_window, period}{Baseline controls, passed through to the same
machinery as \code{\link[=diagnose_batches]{diagnose_batches()}}. \code{period} (e.g. \code{7}) absorbs a scheduled weekly
reporting cadence.}

\item{alpha}{Level for the \code{classification} labels. Default \code{0.05}.}

\item{axis}{Which time axis to scan for arrivals: \code{"report"} (default) or
\code{"validation"}. Needs a validation process (see \code{\link[=add_validation_date]{add_validation_date()}});
cases still \code{"pending"} are left out.}

\item{drop_censored}{Logical. Ignore the rows whose date on \code{axis} is
flagged censored (\code{is_censored_report}, or \code{is_censored_validation} on the
validation axis). Default \code{TRUE}: a censored date is a \emph{bound}, not the
date the record arrived, so those rows would pile up on the censoring date
and be rediscovered as the very batch the censoring already recorded.}
}
\value{
A tibble of class \code{transport_discriminant}, one row per (report date,
stratum), with columns \code{report_date}, \code{stratum}, \code{reported}, \code{baseline},
\code{window_total}, \code{spike} (reported minus baseline), \code{deficit}, \code{delta},
\code{transport_z}, \code{creation_z}, \code{classification} and \code{batch}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Computes, for every report date, the two coordinates of \code{\link[=diagnose_batches]{diagnose_batches()}}'s
conservation law -- the \strong{deficit} (the \emph{transport} axis: how many reports the
preceding window is missing) and the window \strong{discriminant} (the \emph{creation}
axis: the window total relative to its baseline) -- together with their robust
standardised versions \code{transport_z} and \code{creation_z}.
}
\details{
A batch \emph{moves} reports later without creating them, so it leaves a positive
deficit while conserving the window total (\code{transport_z} large, \code{creation_z}
near 0). A genuine surge \emph{creates} reports, lifting the window total without a
deficit (\code{creation_z} large, \code{transport_z} near 0). Reading the two together
separates a backlog release from an epidemic surge: a point sits in the
\strong{batch} corner when its transport score is large and its creation score is
not. A negative \code{creation_z} with no transport is a hold in progress (the
window is depleted and nothing has been released yet). The \code{classification}
column applies these labels at level \code{alpha}, exactly as in \code{\link[=diagnose_batches]{diagnose_batches()}}.
}
\examples{
data(denguedat)
dn <- tbl_now(denguedat, onset_week, report_week, verbose = FALSE)
td <- transport_discriminant(dn)
td[td$batch, ]

}
\seealso{
\code{\link[=diagnose_batches]{diagnose_batches()}} for the hypothesis test, \code{\link[=diagnostic_plot]{diagnostic_plot()}} to plot
this plane.
}
