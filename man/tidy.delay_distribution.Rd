% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy.R
\name{tidy.delay_distribution}
\alias{tidy.delay_distribution}
\alias{tidy.estimate_dist}
\alias{tidy.epidist_fit}
\title{Tidy a fitted delay distribution}
\usage{
\method{tidy}{estimate_dist}(x, probs = NULL, level = 0.95, ...)

\method{tidy}{epidist_fit}(x, probs = NULL, level = 0.95, newdata = NULL, ...)
}
\arguments{
\item{x}{A fit from \code{\link[epidist:epidist]{epidist::epidist()}} or \code{\link[EpiNow2:estimate_dist]{EpiNow2::estimate_dist()}}.}

\item{probs}{Optional numeric vector of probabilities in \verb{[0, 1]}, adding one
\verb{q*} column each.}

\item{level}{Width of the reported interval. Defaults to \code{0.95}.}

\item{...}{Unused, for generic consistency.}

\item{newdata}{\code{tidy.epidist_fit()} only. Optional data frame passed to
\code{\link[epidist:predict_delay_parameters]{epidist::predict_delay_parameters()}}, for a fit with covariates in the
delay model (\code{formula = mu ~ 1 + gender}, say). \code{NULL} uses the fit's own
data.}
}
\value{
A tibble, as described in \emph{Value}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\pkg{epidist} and \code{\link[EpiNow2:estimate_dist]{EpiNow2::estimate_dist()}} (new in \pkg{EpiNow2} 1.9.0) do
not produce a nowcast: they estimate a \strong{reporting-delay distribution}. There
are therefore no per-event-date case estimates to tidy, and the columns
\code{\link[=tidy.nowcast]{tidy.nowcast()}} promises (\code{event_date}, \code{stratum}, ...) would all be
meaningless. Both methods instead return the same \emph{delay-shaped} table: one
row per distribution parameter, with \code{term} rather than \code{event_date}, so the
two packages' fits can be compared side by side.
}
\section{Value}{


A \link[tibble:tibble]{tibble} with one row per parameter of the fitted delay
distribution -- whichever family was fitted, named as the fitting package
names it -- plus the derived \strong{\code{mean}} and \strong{\code{sd}} of the delay, which are
the numbers most people actually want. The columns are:

\describe{
\item{\code{term}}{\code{character}. The parameter: the distribution's own parameters
(\code{mu}, \code{sigma}, ...) plus \code{mean} and \code{sd}.}
\item{\code{estimate}}{\code{numeric}. Posterior median.}
\item{\code{conf.low}, \code{conf.high}}{\code{numeric}. Interval bounds, following
\pkg{broom}'s naming.}
\item{\code{level}}{\code{numeric}. The width of that interval.}
\item{\code{engine}}{\code{character}. \code{"epidist"} or \code{"EpiNow2"}.}
}

Everything is summarised from the posterior draws, so \code{level} is the interval
you asked for rather than whichever \code{CrIs} the fit happened to use, and
\code{probs} appends one \verb{q*} column per requested probability -- a real quantile
rather than an approximation.
}

\section{How \code{mean} and \code{sd} are obtained}{


\pkg{epidist} reports continuous-distribution moments via
\code{epidist::add_mean_sd()}.

\pkg{EpiNow2} gets them without naming a distribution. It can fit five
families today and may add more, and a \code{switch()} in this package would
quietly stop reporting anything the day it does. Instead each draw's
parameters are put back into the fit's own \code{dist_spec} and discretised with
\code{\link[EpiNow2:discretise]{EpiNow2::discretise()}}, which knows the families; the moments are then a
summation over the PMF. A new family works as soon as \code{discretise()} supports
it.

The trade-off is that the \pkg{EpiNow2} numbers are the moments of the
\strong{discretised} delay -- the distribution \pkg{EpiNow2} convolves with
downstream. Against the closed forms the mean is exact and the sd runs about
1\% high, that being the variance a discrete grid adds. Expect a difference of
that order when comparing the two packages' fits.
}

\section{A name collision worth knowing about}{


\code{summary()} on an \code{estimate_dist} fit has \code{mean} and \code{sd} \strong{columns}, and
those are the posterior mean and sd \strong{of the parameter} on that row -- not of
the delay. The \code{mean} and \code{sd} these methods report are \strong{rows}, and are the
delay distribution's own moments. Same words, different quantities.
}

\section{Dispatch}{


\code{epidist()} returns an object of class \code{c("brmsfit", "epidist_fit")}, in that
order, so if \pkg{broom.mixed} is loaded its \code{tidy.brmsfit()} method matches
\strong{first} and you get raw \pkg{brms} parameters instead of this table. Call
\code{tidy.epidist_fit(fit)} explicitly when you want the delay distribution and
cannot be sure which method will win.
}

\examples{
\dontshow{if (requireNamespace("EpiNow2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(denguedat)
# A short window: fitting a delay distribution does not need twenty years of
# data, and Stan is slow.
recent <- subset(denguedat, onset_week >= as.Date("2010-06-01"))
nowobj <- tbl_now(recent,
  event_date = "onset_week", report_date = "report_week", verbose = FALSE
)

# `target = "estimate_dist"` gives the censored linelist EpiNow2 wants: one
# row per case, each date as the interval it is known to fall in.
delays <- tbl_now_to_EpiNow2(nowobj,
  target = "estimate_dist", verbose = FALSE, quiet = TRUE
)
head(delays)

# A short chain keeps the example quick -- use EpiNow2's defaults for real
## work. `try()` guards the case where EpiNow2 is installed but its Stan
# toolchain is not.
fit <- try(
  EpiNow2::estimate_dist(
    delays,
    stan = EpiNow2::stan_opts(samples = 100, chains = 1)
  ),
  silent = TRUE
)

# One row per fitted parameter, plus the delay's own mean and sd.
if (!inherits(fit, "try-error")) {
  print(tidy(fit))
  print(tidy(fit, probs = c(0.05, 0.95)))
}
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=tbl_now_to_epidist]{tbl_now_to_epidist()}} and \code{\link[=tbl_now_to_EpiNow2]{tbl_now_to_EpiNow2()}} for the conversions;
\link[=tidy.nowcast]{tidy()} for tidying a \emph{case-count} nowcast rather than a delay
distribution; \link{revision_delay} for the delay these are estimating.
}
