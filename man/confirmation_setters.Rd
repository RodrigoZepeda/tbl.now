% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confirmation.R
\name{confirmation_setters}
\alias{confirmation_setters}
\alias{add_confirmation}
\alias{change_confirmation}
\alias{remove_confirmation}
\title{Attach, change or drop a confirmation process}
\usage{
add_confirmation(
  x,
  confirmation_date,
  confirmation_type = NULL,
  confirmation_units = "auto"
)

change_confirmation(
  x,
  confirmation_date,
  confirmation_type = NULL,
  confirmation_units = "auto"
)

remove_confirmation(x)
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{confirmation_date}{The confirmation-date column (tidy-select: a bare
name or a string).}

\item{confirmation_type}{Optional column holding \code{"confirmed"},
\code{"retracted"} or \code{"pending"}. When you leave it out, rows with a
confirmation date get \code{NA} and a warning: a date on its own cannot say
whether the test came back positive or negative.}

\item{confirmation_units}{\code{"auto"} (default) infers the grid from the column,
as \code{event_units} does.}
}
\value{
A \code{tbl_now}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A confirmation is the \strong{third} date in a surveillance record: after the event
happened and after it was reported, somebody decided whether it was real. For
influenza that is the laboratory result -- and it can come back negative, in
which case the case is \emph{retracted} rather than confirmed.

\code{add_confirmation()} attaches one to an object that has none;
\code{change_confirmation()} replaces whatever is there; \code{remove_confirmation()}
drops it, leaving an ordinary two-date \code{tbl_now}.
}
\section{What attaching one changes}{

\itemize{
\item \strong{\code{now} moves.} A confirmation is an observation, so the as-of moment
becomes the latest of the report and confirmation dates. Validation refuses
an object whose \code{now} falls before a confirmation that has already
happened.
\item \strong{Two columns appear.} \code{.confirmation_num} is the date on the same numeric
anchor as \code{.event_num}/\code{.report_num}; \code{.confirmation_delay} is the time
from report to resolution. Both are protected, like \code{.delay}.
\item \strong{Counting gains a dimension.} \code{\link[=to_count]{to_count()}} groups by the confirmation
date and outcome as well, so a confirmed and a retracted case on the same
\verb{(event, report)} pair stay separate rather than being summed together.
\item \strong{The timeline is checked.} \verb{event_date <= report_date <= confirmation_date}; rows that break it are warned about, not silently
accepted.
}
}

\examples{
data(hai_bucaramanga)

## specimen taken -> reported -> (here) the laboratory receipt as the
# confirmation step.
hai <- hai_bucaramanga |>
  dplyr::filter(
    !is.na(specimen_date), !is.na(report_date), !is.na(received_date)
  ) |>
  tbl_now(
    event_date = specimen_date, report_date = report_date,
    data_type = "linelist", verbose = FALSE
  )

hai <- suppressWarnings(add_confirmation(hai, received_date))
has_confirmation(hai)
get_confirmation_date(hai)

# A date alone cannot say whether the case was confirmed or retracted, which
# is why the call above warns. Supplying the outcome column removes the doubt.
hai$outcome <- ifelse(seq_len(nrow(hai)) \%\% 10 == 0, "retracted", "confirmed")
hai <- change_confirmation(hai, received_date, confirmation_type = outcome)
get_confirmation_type(hai)
table(hai[[get_confirmation_type(hai)]])

# Dropping it leaves an ordinary two-date object.
hai <- remove_confirmation(hai)
has_confirmation(hai)

}
\seealso{
\link{confirmation_getters} to read the attributes back;
\link{confirmation_counts} to count confirmed, retracted and pending cases;
\link{confirmation_delay} and \code{\link[=diagnose_confirmation_delay]{diagnose_confirmation_delay()}} for how long
resolution takes; \code{\link[=plot_confirmation_status]{plot_confirmation_status()}} to see it;
\code{\link[=add]{add()}} for the event- and report-date attributes.
}
