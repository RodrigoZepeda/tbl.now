% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_methods.R, R/run_nowcast.R
\name{nowcast_fit.diseasenowcasting}
\alias{nowcast_fit.diseasenowcasting}
\alias{nowcast_fit.baselinenowcast}
\alias{nowcast_fit.epinowcast}
\alias{nowcast_fit.NobBS}
\alias{nowcast_fit.surveillance}
\alias{nowcast_fit.EpiNow2}
\alias{nowcast_fit}
\alias{nowcast_fit.default}
\title{Fit a nowcast with one modelling package}
\usage{
\method{nowcast_fit}{diseasenowcasting}(
  method,
  x,
  ...,
  quantile_levels = nowcast_quantile_levels(),
  verbose = TRUE
)

\method{nowcast_fit}{baselinenowcast}(
  method,
  x,
  ...,
  draws = 1000,
  delays_unit = NULL,
  quantile_levels = nowcast_quantile_levels(),
  verbose = TRUE
)

\method{nowcast_fit}{epinowcast}(
  method,
  x,
  ...,
  preprocess_args = list(),
  quantile_levels = nowcast_quantile_levels(),
  verbose = TRUE
)

\method{nowcast_fit}{NobBS}(
  method,
  x,
  ...,
  specs = list(),
  quantile_levels = nowcast_quantile_levels(),
  verbose = TRUE
)

\method{nowcast_fit}{surveillance}(
  method,
  x,
  ...,
  when = NULL,
  D = NULL,
  fit_method = "bayes.notrunc.bnb",
  control = list(),
  quantile_levels = nowcast_quantile_levels(),
  verbose = TRUE
)

\method{nowcast_fit}{EpiNow2}(
  method,
  x,
  ...,
  convert_args = list(),
  quantile_levels = nowcast_quantile_levels(),
  verbose = TRUE
)

nowcast_fit(
  method,
  x,
  ...,
  quantile_levels = nowcast_quantile_levels(),
  verbose = TRUE
)

\method{nowcast_fit}{default}(
  method,
  x,
  ...,
  quantile_levels = nowcast_quantile_levels(),
  verbose = TRUE
)
}
\arguments{
\item{method}{A \code{\link[=engine]{engine()}} object. (The argument is called \code{method} for
historical reasons and because that is what it selects; what arrives is the
engine, so \code{method$args}, \code{method$label} and the rest are available to a
backend that wants them.)}

\item{x}{A \code{tbl_now} object.}

\item{...}{Arguments passed straight to the underlying modelling function.
\code{\link[=run_nowcast]{run_nowcast()}} splices the engine's own arguments in here.}

\item{quantile_levels}{Numeric vector of probabilities. Most backends ignore
it at fit time (the quantiles are computed from the draws afterwards), but
some need to be told up front which levels to report.}

\item{verbose}{Logical. Whether the backend (and the converters feeding it)
should be chatty.}

\item{draws}{(\code{"baselinenowcast"} only) Number of nowcast samples to draw.}

\item{delays_unit}{(\code{"baselinenowcast"} only) Unit of the reporting
triangle's delay axis; inferred from the object's time units when \code{NULL}.}

\item{preprocess_args}{(\code{"epinowcast"} only) A list of arguments for
\code{\link[=tbl_now_to_epinowcast]{tbl_now_to_epinowcast()}}, e.g. \code{list(max_delay = 20)}.}

\item{specs}{(\code{"NobBS"} only) The \code{specs} list of \code{NobBS::NobBS()}. The
\code{quantiles} element is filled from \code{quantile_levels} unless you set it.}

\item{when, D, fit_method, control}{(\code{"surveillance"} only) The \code{when}, \code{D},
\code{method} and \code{control} arguments of \code{\link[surveillance:nowcast]{surveillance::nowcast()}}. \code{when}
defaults to \code{get_surveillance_when(x, length = D + 1)}, \code{D} to the largest
delay in the data, and \code{control$dRange} to \code{\link[=get_surveillance_range]{get_surveillance_range()}} --
the grid running to \code{\link[=get_now]{get_now()}}, which a line list cannot express on its
own. Both grids are built from the whole object, so every stratum is fitted
on the same time axis. \code{fit_method} is \pkg{surveillance}'s \code{method}
argument, renamed so it cannot collide with \code{\link[=run_nowcast]{run_nowcast()}}'s own \code{method}.}

\item{convert_args}{(\code{"EpiNow2"} only) A list of arguments for
\code{\link[=tbl_now_to_EpiNow2]{tbl_now_to_EpiNow2()}}, e.g. \code{list(accumulate = FALSE)}.}
}
\value{
\code{nowcast_fit()} returns the modelling package's own object, verbatim.
It is stored in the \code{fit} property of the resulting \link{tbl_nowcast}, and it is
the only thing \code{\link[=nowcast_tidy]{nowcast_tidy()}} is given besides the \code{tbl_now} itself, so
put whatever the tidying step will need into it.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{nowcast_fit()} and \code{\link[=nowcast_tidy]{nowcast_tidy()}} are the two extension points of the
nowcasting framework. Together they teach \code{\link[=run_nowcast]{run_nowcast()}} about a new
modelling package: \code{nowcast_fit()} runs the model, \code{nowcast_tidy()} turns
whatever it returned into the tidy quantile format every other function in
tbl.now understands.

Dispatch happens on the object built by \code{\link[=engine]{engine()}}, so a method for
\code{"mypackage"} is a function called \code{nowcast_fit.mypackage()}. It can live in
any package.
}
\examples{
# A minimal backend: two S3 methods and you are done.
nowcast_fit.constant <- function(method, x, ..., quantile_levels, verbose = TRUE) {
  counts <- get_latest_reported_cases(x)
  list(dates = counts[[get_event_date(x)]], value = counts[[ncol(counts)]])
}

nowcast_tidy.constant <- function(method, fit, x, ..., quantile_levels) {
  predictions <- tidyr::expand_grid(
    event_date = fit$dates, .quantile_level = quantile_levels
  )
  predictions$.value <- rep(fit$value, each = length(quantile_levels))
  names(predictions)[1] <- get_event_date(x)
  list(predictions = predictions, draws = NULL)
}

}
\seealso{
\code{\link[=nowcast_tidy]{nowcast_tidy()}}, \code{\link[=run_nowcast]{run_nowcast()}}, \code{\link[=list_nowcast_methods]{list_nowcast_methods()}} and
\code{vignette("custom-nowcast-models")} for a worked example of a new backend.
}
