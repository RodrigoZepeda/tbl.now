% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tidy_nowcast.R
\name{tidy.tbl_nowcast}
\alias{tidy.tbl_nowcast}
\alias{tidy_tbl_nowcast}
\title{Tidy a nowcast produced by \code{run_nowcast()} or \code{nowcast_ensemble()}}
\arguments{
\item{x}{A \link{tbl_nowcast}.}

\item{probs}{Optional numeric vector of probabilities in \verb{[0, 1]}, adding one
\verb{q*} column each. Only available when the nowcast carries \strong{draws}: a
quantile-only nowcast cannot produce a level it was not summarised at, so
asking for one is an error rather than an interpolation dressed up as a
quantile.}

\item{...}{Unused, for generic consistency.}
}
\value{
A tibble, as described in \emph{Value}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Turns a \link{tbl_nowcast} into the same table every other \code{tidy()} method in this
package returns, so a nowcast produced through \code{\link[=run_nowcast]{run_nowcast()}} and one
produced by calling a modelling package by hand are read the same way.
}
\section{Value}{


A \link[tibble:tibble]{tibble} with the columns documented at \code{\link[=tidy.nowcast]{tidy.nowcast()}}:
\code{event_date}, \code{stratum}, \code{estimate}, \code{conf.low}, \code{conf.high}, \code{level} and
\code{engine}, plus one \verb{q*} column per element of \code{probs}.

Two of those columns are read off the object rather than assumed:

\describe{
\item{\code{level}}{A \code{tbl_nowcast} holds whatever quantile levels it was
summarised at, and those need not be symmetric. \code{level} is the width of
the \strong{widest symmetric pair actually present} -- \code{0.95} for the default
\code{\link[=nowcast_quantile_levels]{nowcast_quantile_levels()}}, \code{0.8} for a fit summarised at
\code{c(0.1, 0.5, 0.9)}. When no symmetric pair exists, \code{level}, \code{conf.low}
and \code{conf.high} are all \code{NA}: a guessed width defeats the point of the
column.}
\item{\code{engine}}{The nowcast's \code{method}, so \code{"baselinenowcast"} for a single
fit and \code{"ensemble"} (or whatever \code{name} \code{\link[=nowcast_ensemble]{nowcast_ensemble()}} was given)
for a combined one.}
}

\code{stratum} is \code{"all"} only when the nowcast declares no strata. Several strata
columns are pasted \code{" | "}-separated, matching the rest of the package, so
\verb{(stratum, event_date)} is a unique key.

\code{estimate} is the \code{0.5} quantile, and \code{NA} when the nowcast was summarised at
levels that do not include the median.
}

\examples{
predictions <- tidyr::expand_grid(
  onset_week = as.Date("2020-01-05") + c(0, 7),
  .quantile_level = c(0.025, 0.5, 0.975)
)
predictions$.value <- c(5, 10, 18, 6, 12, 21)
nc <- tbl_nowcast(
  predictions = predictions, method = "toy", event_date = "onset_week"
)

tidy(nc)

}
\seealso{
\code{\link[=tidy.nowcast]{tidy.nowcast()}} for the same table off a raw engine fit,
\code{\link[=run_nowcast]{run_nowcast()}}, \code{\link[=nowcast_ensemble]{nowcast_ensemble()}}.
}
