% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/censor_delays.R
\name{censor_delays_above}
\alias{censor_delays_above}
\title{Flag reports with an implausibly long delay as censored}
\usage{
censor_delays_above(data, max_delay, quiet = FALSE)
}
\arguments{
\item{data}{A \code{tbl_now} object.}

\item{max_delay}{Numeric. Reports with delay strictly greater than this
(in the data's event units) are flagged as censored.}

\item{quiet}{If \code{TRUE}, suppress the informational message.}
}
\value{
The \code{tbl_now} with its \code{is_censored} column updated (created if
absent).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Marks every report whose reporting delay exceeds \code{max_delay} (in event units)
as \emph{censored}: the recorded delay becomes an upper bound rather than an exact
value. This is the natural response to a "delay too long" outlier: instead of
letting, say, a 300-day delay drag the delay distribution, you tell the model
only that the case arrived \emph{by} that delay.

The delay is read from the \code{tbl_now}'s generated \code{.delay} column (the report
date minus the event date, in the data's event units). The \code{is_censored}
column is updated (or created) and merged with any existing censoring flags
(already-censored reports stay censored).
}
\examples{
df <- data.frame(
  onset = as.Date("2020-01-01") + c(0, 0, 1, 2),
  reported = as.Date("2020-01-01") + c(1, 5, 2, 300)
)
tn <- tbl_now(df,
  event_date = onset, report_date = reported,
  data_type = "linelist", verbose = FALSE
)

# the 300-day report becomes censored (an upper bound on its delay)
tn <- censor_delays_above(tn, max_delay = 60)
}
\seealso{
\code{\link[=add_is_censored]{add_is_censored()}}, \code{\link[=change_is_censored]{change_is_censored()}}, \code{\link[=remove_is_censored]{remove_is_censored()}}
}
