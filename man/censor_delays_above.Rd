% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/censor_delays.R, R/confirmation.R
\name{censor_delays_above}
\alias{censor_delays_above}
\alias{censor_confirmation_delays_above}
\title{Treat implausibly long delays as censored rather than exact}
\usage{
censor_delays_above(x, max_delay, verbose = TRUE)

censor_confirmation_delays_above(x, max_delay, verbose = TRUE)
}
\arguments{
\item{x}{A \code{tbl_now} object. \code{censor_confirmation_delays_above()} requires one
that carries a confirmation process (see \link[=confirmation_setters]{add_confirmation()}).}

\item{max_delay}{Numeric. Delays strictly greater than this are censored, in
the object's event units (reporting) or confirmation units (confirmation).}

\item{verbose}{Logical. Whether to report how many rows were affected.
Default \code{TRUE}.}
}
\value{
\code{censor_delays_above()} returns the \code{tbl_now} with its \code{is_censored} column
updated, creating it when absent.

\code{censor_confirmation_delays_above()} returns the \code{tbl_now} with the offending
rows' \code{confirmation_type} set to \code{"pending"} and their confirmation date set to
\code{NA} -- a resolution you refuse to believe is not a resolution.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Every surveillance system has a handful of records that arrive absurdly late
-- a case with onset in March that turns up in December. Taken at face value
those delays drag the estimated delay distribution to the right and make the
nowcast think reporting is far slower than it is.

Rather than deleting those records (which throws away a real case) these
functions mark them as \strong{censored}: the object keeps the case, but records
its delay as \emph{"at least this long"} instead of \emph{"exactly this long"}.
\itemize{
\item \code{censor_delays_above()} does this for the \strong{reporting} delay, by setting the
\code{is_censored} flag.
\item \code{censor_confirmation_delays_above()} does the same for the \strong{confirmation}
delay -- a case still waiting on a laboratory result months later is, in
practice, never going to be resolved -- by returning it to \code{"pending"}.
}
}
\details{
The reporting delay is read from the generated \code{.delay} column (report date
minus event date, in the object's event units). Existing censoring flags are
merged rather than overwritten, so a report that was already censored stays
censored.
}
\examples{
# Four cases, one of which took 300 days to be reported.
df <- data.frame(
  onset = as.Date("2020-01-01") + c(0, 0, 1, 2),
  reported = as.Date("2020-01-01") + c(1, 5, 2, 300)
)
tn <- tbl_now(df,
  event_date = onset, report_date = reported,
  data_type = "linelist", verbose = FALSE
)
tn$.delay

# Anything slower than 60 days is recorded as a lower bound, not a fact.
censored <- censor_delays_above(tn, max_delay = 60)
censored[[get_is_censored(censored)]]

# The confirmation counterpart: a laboratory result that took 90 days.
cases <- data.frame(
  onset = as.Date("2021-01-04") + 0:4,
  visit = as.Date("2021-01-05") + 0:4,
  result = as.Date("2021-01-05") + 0:4 + c(1, 2, 1, 90, 2),
  outcome = rep("confirmed", 5)
)
flu <- tbl_now(cases,
  event_date = onset, report_date = visit,
  confirmation_date = result, confirmation_type = outcome,
  data_type = "linelist", verbose = FALSE
)

# That one goes back to "pending"; the other four stay confirmed.
table(censor_confirmation_delays_above(flu, 30, verbose = FALSE)[["outcome"]])

}
\seealso{
\link[=add]{add_is_censored()} and \link[=add]{change_is_censored()} to set the flag by
hand; \code{\link[=diagnose_confirmation_delay]{diagnose_confirmation_delay()}} and \code{\link[=plot_delay_distribution]{plot_delay_distribution()}} to find
the threshold worth using; \code{\link[=diagnose_truncation]{diagnose_truncation()}} for the delays that are
missing rather than long; \code{\link[=complete_zeroes]{complete_zeroes()}} for the opposite problem.
}
