% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnose.R
\name{nowcast_diagnose_components}
\alias{nowcast_diagnose_components}
\alias{diagnose_declarations}
\alias{diagnose_ordering}
\alias{diagnose_missing}
\alias{diagnose_duplicates}
\alias{diagnose_units}
\alias{diagnose_negatives}
\alias{diagnose_now}
\alias{diagnose_truncation}
\alias{diagnose_strata}
\title{Individual blocks of a \code{tbl_now} diagnosis}
\usage{
diagnose_declarations(x, by_strata = NULL, strata = NULL)

diagnose_ordering(x, by_strata = NULL, strata = NULL)

diagnose_missing(x, by_strata = NULL, strata = NULL)

diagnose_duplicates(
  x,
  by_strata = NULL,
  strata = NULL,
  warn_non_uniqueness = TRUE
)

diagnose_units(x, by_strata = NULL, strata = NULL)

diagnose_negatives(x, by_strata = NULL, strata = NULL)

diagnose_now(x, by_strata = NULL, strata = NULL)

diagnose_truncation(x, by_strata = NULL, strata = NULL)

diagnose_strata(x, by_strata = NULL, strata = NULL)
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{by_strata}{Logical. Add one set of rows per stratum, for the checks
that are naturally per-stratum (missingness, negative increments,
right-truncation, the gap to \code{now}). Defaults to \code{TRUE} when the object has
strata. The checks that are statements about the object as a whole
(declarations, units, duplicates, ordering) are always reported once, with
\code{stratum = "all"}.}

\item{strata}{Character vector of columns to stratify by. Defaults to
\code{get_strata(x)}.}

\item{warn_non_uniqueness}{Logical. Run the duplicate-row check. Defaults to
\code{TRUE} here, unlike \code{\link[=validate_tbl_now]{validate_tbl_now()}}, where it defaults to \code{FALSE}
because it runs on every \code{dplyr} verb.}
}
\value{
A tibble in the schema documented in \code{\link[=diagnose]{diagnose()}}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Each function returns one block of \code{\link[=diagnose]{diagnose()}}, in the same schema, so they
can be stacked with \code{\link[dplyr:bind_rows]{dplyr::bind_rows()}} or used on their own.
\itemize{
\item \code{diagnose_declarations()} -- the attributes and the columns they name:
types, existence, collisions, columns the object was never told about, and
temporal effects that were added but never materialised.
\item \code{diagnose_ordering()} -- the \verb{event <= report <= validation} timeline.
\item \code{diagnose_missing()} -- \code{NA} values, per column and per stratum. An \code{NA}
\emph{count} is reported neutrally: in a reporting triangle it means \emph{not yet
observed}, which is correct data rather than a defect.
\item \code{diagnose_duplicates()} -- rows that repeat on the full key.
\item \code{diagnose_units()} -- the declared units against each other, against the
calendar the dates actually land on, and against the delay they produce.
\item \code{diagnose_negatives()} -- negative counts, and the negative increments a
downward revision leaves behind when cumulative data is de-accumulated.
\item \code{diagnose_now()} -- anything dated after \code{now}, and how stale the object is.
\item \code{diagnose_truncation()} -- how many recent event dates are still immature,
and how much of their eventual total is probably still missing.
\item \code{diagnose_strata()} -- the smallest and the sparsest stratum, and the
validations still pending.
}
}
\examples{
data(denguedat)
ndata <- tbl_now(denguedat,
  event_date = "onset_week",
  report_date = "report_week",
  strata = "gender",
  verbose = FALSE
)

# Is the object described correctly, and do the dates make sense?
diagnose_declarations(ndata)
diagnose_ordering(ndata)
diagnose_units(ndata)
diagnose_now(ndata)

# Is anything missing, repeated, negative, or cut off at the recent edge?
diagnose_missing(ndata)
diagnose_duplicates(ndata)
diagnose_negatives(ndata)
diagnose_truncation(ndata)

# Are the strata usable?
diagnose_strata(ndata)

## Each returns the same schema, so they stack the way diagnose() stacks them.
dplyr::bind_rows(
  diagnose_units(ndata),
  diagnose_now(ndata)
)

}
\seealso{
\code{\link[=diagnose]{diagnose()}}, which stacks all of these and sorts them worst-first;
\code{\link[=validate_tbl_now]{validate_tbl_now()}} for the same findings raised as errors and warnings;
\link{nowcast_summary_components} for what \emph{is} in the data rather than what is
wrong with it; \code{\link[=diagnose_drift]{diagnose_drift()}}, \code{\link[=diagnose_changepoint]{diagnose_changepoint()}} and
\code{\link[=diagnose_batches]{diagnose_batches()}} for the statistical tests \code{diagnose()} deliberately does
not run. The
\href{https://rodrigozepeda.github.io/tbl.now/articles/diagnosing-a-tbl-now.html}{\emph{Diagnosing a tbl_now} article}
explains how to read each finding.
}
