% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_ensemble.R
\name{nowcast_ensemble}
\alias{nowcast_ensemble}
\title{Combine several nowcasts into an ensemble}
\usage{
nowcast_ensemble(
  ...,
  type = c("quantile", "linear_pool"),
  weights = "equal",
  backtest = NULL,
  quantile_levels = NULL,
  n_draws = 4000L,
  name = "ensemble",
  verbose = TRUE
)
}
\arguments{
\item{...}{\link{tbl_nowcast} objects, or a single list of them. Named arguments
rename the members.}

\item{type}{How to combine the members:

\describe{
\item{\code{"quantile"} (default)}{Average the members' predictive quantiles
level by level (Vincentization). Applicable to every backend.}
\item{\code{"linear_pool"}}{Pool the members' posterior draws into a mixture
distribution and re-summarise it. Requires that every member returned
draws, and generally yields wider intervals.}
}}

\item{weights}{Either the string \code{"equal"} (default), a numeric vector (named
by method, or in the order the members were given), or one of
\code{"inverse_score"} / \code{"optim"}. The last two require \code{backtest} and are
passed to \code{\link[=nowcast_weights]{nowcast_weights()}}.}

\item{backtest}{A \code{\link[=nowcast_backtest]{nowcast_backtest()}} object, required when \code{weights} is
\code{"inverse_score"} or \code{"optim"}.}

\item{quantile_levels}{Quantile levels to report the ensemble at. Defaults to
the levels shared by all members.}

\item{n_draws}{Number of draws in the pooled sample when
\code{type = "linear_pool"}. Default \code{4000}.}

\item{name}{Name to record as the ensemble's \code{method}. Default
\code{"ensemble"}.}

\item{verbose}{Logical. Whether to report the weights that were used.}
}
\value{
A \link{tbl_nowcast} whose \code{fit} property is the list of member nowcasts
and whose \code{metadata} holds the \code{weights} and the combination \code{type}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Takes the nowcasts produced by different modelling packages on the \emph{same}
\code{tbl_now} and combines them into a single \link{tbl_nowcast}. Ensembles are
routinely better calibrated than any of their members, and because
\code{\link[=run_nowcast]{run_nowcast()}} puts every backend on the same tidy footing, combining them
needs no reshaping on your side.
}
\examples{
toy <- function(method, shift) {
  predictions <- data.frame(
    onset_week = as.Date("2020-01-05"),
    .quantile_level = c(0.25, 0.5, 0.75),
    .value = c(8, 10, 13) + shift
  )
  tbl_nowcast(predictions = predictions, method = method, event_date = "onset_week")
}

nowcast_ensemble(toy("a", 0), toy("b", 4), verbose = FALSE)

# Unequal weights
nowcast_ensemble(toy("a", 0), toy("b", 4), weights = c(a = 0.75, b = 0.25), verbose = FALSE)

}
\seealso{
\code{\link[=run_nowcast]{run_nowcast()}} to produce the nowcasts being combined;
\code{\link[=nowcast_backtest]{nowcast_backtest()}} and \code{\link[=nowcast_weights]{nowcast_weights()}} to decide how much to trust each
one, instead of weighting them equally;
\code{\link[=score_nowcast]{score_nowcast()}} to check the ensemble beats its members. The
\href{https://rodrigozepeda.github.io/tbl.now/articles/ensemble-nowcasting.html}{\emph{One call, many models} article}
builds one end to end.
}
