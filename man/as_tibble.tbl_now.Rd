% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_temporal_effects.R
\name{as_tibble.tbl_now}
\alias{as_tibble.tbl_now}
\alias{as_tibble.grouped_tbl_now}
\alias{as.data.frame.tbl_now}
\alias{as.data.frame.grouped_tbl_now}
\title{Coerce a \code{tbl_now} to a tibble or a data frame}
\usage{
\method{as_tibble}{tbl_now}(x, ..., compute_temporal_effects = FALSE)

\method{as_tibble}{grouped_tbl_now}(x, ..., compute_temporal_effects = FALSE)

\method{as.data.frame}{tbl_now}(x, ..., compute_temporal_effects = FALSE)

\method{as.data.frame}{grouped_tbl_now}(x, ..., compute_temporal_effects = FALSE)
}
\arguments{
\item{x}{A \code{tbl_now} (or \code{grouped_tbl_now}) object.}

\item{...}{Passed on to the underlying \code{\link[tibble:as_tibble]{tibble::as_tibble()}} /
\code{\link[base:as.data.frame]{base::as.data.frame()}}.}

\item{compute_temporal_effects}{Logical (default \code{FALSE}). When \code{TRUE},
materialise the lazy \code{\link[=temporal_effects]{temporal_effects()}} spec before coercing so the
temporal-effect columns are included.}
}
\value{
A \code{tibble} (for \code{as_tibble()}) or a \code{data.frame} (for
\code{as.data.frame()}); it carries the temporal-effect columns when
\code{compute_temporal_effects = TRUE}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{\link[tibble:as_tibble]{tibble::as_tibble()}} and \code{\link[=as.data.frame]{as.data.frame()}} methods for \code{tbl_now}. They drop
the \code{tbl_now} class and metadata, returning a plain \code{tibble} / \code{data.frame}.

Set \code{compute_temporal_effects = TRUE} to \strong{materialise the lazy
\code{\link[=temporal_effects]{temporal_effects()}} specification} with \code{\link[=compute_temporal_effects]{compute_temporal_effects()}} first,
so the holiday / Fourier / calendar columns are present in the result. The
input \code{tbl_now} is left unchanged (the spec is materialised on a copy).

The default is \code{FALSE} on purpose: \pkg{dplyr} uses these coercions
internally as cheap declassers (for example inside \code{group_by()} and the data
mask behind \code{mutate()} / \code{filter()} / \code{slice()}). Materialising there would
both break the lazy \code{temporal_effects} contract and recurse, so materialising
is strictly opt-in. To get a modelling-ready frame with the effects computed,
either pass \code{compute_temporal_effects = TRUE} here or call
\code{\link[=compute_temporal_effects]{compute_temporal_effects()}} on the \code{tbl_now} beforehand.
}
\examples{
data(denguedat)
df_now <- tbl_now(denguedat,
  event_date  = onset_week,
  report_date = report_week,
  t_effects   = temporal_effects(week_of_year = TRUE),
  verbose     = FALSE
)

## Plain coercion leaves the spec lazy (no temporal-effect columns):
".event_week_of_year" \%in\% names(tibble::as_tibble(df_now)) # FALSE

# Opt in to materialise the holiday / Fourier / calendar columns:
as_tibble(df_now, compute_temporal_effects = TRUE)
as.data.frame(df_now, compute_temporal_effects = TRUE)

}
\seealso{
\code{\link[=compute_temporal_effects]{compute_temporal_effects()}}, \code{\link[=temporal_effects]{temporal_effects()}}
}
