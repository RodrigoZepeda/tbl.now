% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/example_engine.R
\name{example_engine}
\alias{example_engine}
\alias{nowcast_fit.example}
\alias{nowcast_tidy.example}
\title{A toy engine for examples}
\usage{
example_engine(
  ...,
  spread = 0.2,
  min_date = NULL,
  quantile_levels = nowcast_quantile_levels(),
  label = NULL
)

\method{nowcast_fit}{example}(
  engine,
  x,
  ...,
  spread = 0.2,
  quantile_levels = nowcast_quantile_levels(),
  verbose = TRUE
)

\method{nowcast_tidy}{example}(engine, fit, x, ..., quantile_levels)
}
\arguments{
\item{...}{Ignored. Present so the engine accepts the same shape of call as
the real ones.}

\item{spread}{Non-negative number setting the width of the interval, as a
fraction of the point estimate. \code{0} gives a point mass at the median.}

\item{min_date}{How much history to fit on. One of
\itemize{
\item \code{NULL} (default) -- the whole series;
\item a \strong{\code{Date}} -- keep event dates on or after it;
\item a \strong{single number} -- keep the last \emph{n} periods before the object's
\code{now}, counted in the object's event units.
}

The number is usually what you want in a \code{\link[=nowcast_backtest]{nowcast_backtest()}}: \code{now} moves
between fits, and a fixed calendar date would make the fitted window grow as
the backtest walks forward, so the last fit would be trained on more data
than the first. Trimming is per engine on purpose -- \code{baselinenowcast} and
\code{diseasenowcasting} take a long series in their stride, while \code{epinowcast}
scales with the number of reference dates and is best given a window.}

\item{quantile_levels}{Numeric vector of probabilities to report the nowcast
at. Defaults to \code{\link[=nowcast_quantile_levels]{nowcast_quantile_levels()}}.

It lives on the engine because for some backends it is a \strong{fit-time model
argument}, not a way of summarising afterwards. \pkg{NobBS} computes
exactly the quantiles it is handed in \code{specs$quantiles} and keeps no draws,
so a level it was never asked for cannot be recovered, and
\pkg{surveillance} reports a fixed set and warns rather than interpolating.
The draw-keeping backends -- \code{baselinenowcast}, \code{diseasenowcasting},
\code{epinowcast} and \code{EpiNow2} -- answer any level after the fact.}

\item{label}{Name for this engine in a \code{\link[=nowcast_backtest]{nowcast_backtest()}} and in the
ensemble weights derived from one. Defaults to the method name. Give one
when the same package appears twice with different settings, which is the
whole reason two \code{diseasenowcasting} models can be weighted separately.}

\item{engine}{An \code{\link[=engine]{engine()}} object -- the modelling package plus its
arguments. S3 dispatch is on its class, so a backend for \code{"mypackage"} is a
function called \code{nowcast_fit.mypackage()}. The whole engine arrives, not
just its name, so \code{engine$args}, \code{engine$label} and the rest are available
to a backend that wants them.}

\item{x}{A \code{tbl_now} object.}

\item{verbose}{Logical. Whether the backend (and the converters feeding it)
should be chatty.}

\item{fit}{The object returned by \code{\link[=nowcast_fit]{nowcast_fit()}}.}
}
\value{
A \code{nowcast_engine} object, as \code{\link[=engine]{engine()}} returns, that
\code{\link[=run_nowcast]{run_nowcast()}} and \code{\link[=nowcast_backtest]{nowcast_backtest()}} accept.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A deliberately naive nowcasting engine that needs no modelling package. It
exists so that the examples in this package can actually run: every real
engine depends on \pkg{epinowcast}, \pkg{NobBS}, \pkg{EpiNow2} or another
optional package, and an example that cannot run teaches nothing.

\strong{Do not nowcast with this.} It does not model the reporting delay at all --
it reports the counts that have arrived so far and puts a fixed percentage
band around them. Because late reports are exactly what it ignores, it
under-predicts recent dates by design, which is a useful thing to \emph{see} and a
terrible thing to rely on. For real work use one of the
\link[=nowcast_engines]{engines} -- \link[=nowcast_engines]{engine_baselinenowcast()},
\link[=nowcast_engines]{engine_epinowcast()}, \link[=nowcast_engines]{engine_nobbs()} and
the rest -- or write your own.
}
\details{
For each event date (and stratum) it takes the cumulative count reported by
\code{now}, from \link[=get_latest_first]{get_latest_reported_cases()}, and reports that
as the median. The other quantile levels are that median scaled linearly by
\code{spread}, so the 2.5\% and 97.5\% levels sit at roughly \verb{1 -/+ spread} times it.

No random numbers are involved, so it gives the same answer every time and
does not disturb the RNG stream.
}
\examples{
data(denguedat)
recent <- subset(denguedat, onset_week >= as.Date("2010-06-01"))
dengue <- tbl_now(recent,
  event_date = onset_week, report_date = report_week, verbose = FALSE
)

# It is an ordinary engine, so it goes where a real one goes.
example_engine()

nc <- run_nowcast(dengue, example_engine(), verbose = FALSE)
nc

# `spread` controls how wide the (made-up) interval is.
run_nowcast(dengue, example_engine(spread = 0.5), verbose = FALSE)

}
\seealso{
\link{nowcast_engines} for the engines you would actually nowcast with;
\code{\link[=engine]{engine()}} for the general constructor;
\code{\link[=nowcast_fit]{nowcast_fit()}} and \code{\link[=nowcast_tidy]{nowcast_tidy()}}, the two methods this implements -- read
its source for the shortest possible complete backend. The
\href{https://rodrigozepeda.github.io/tbl.now/articles/custom-nowcast-models.html}{\emph{Adding your own nowcasting model} article}
walks through writing a real one.
}
