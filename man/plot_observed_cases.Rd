% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_effects.R
\name{plot_observed_cases}
\alias{plot_observed_cases}
\title{Observed epidemic process with the incompleteness line}
\usage{
plot_observed_cases(x, ...)
}
\arguments{
\item{x}{A \code{\link[=tbl_now]{tbl_now()}} object.}

\item{...}{Further arguments passed to \code{\link[=autoplot.tbl_now]{autoplot.tbl_now()}}, e.g. \code{level},
\code{by_strata}, \code{strata}, \code{event_date_xlim}, \code{plotly} or \code{palette}.}
}
\value{
A \pkg{ggplot2} object (or a \pkg{plotly} widget when \code{plotly = TRUE}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The \code{"epidemic"} panel of \code{\link[=autoplot]{autoplot()}}, drawn on its own: the latest reported
counts per \code{event_date}, with a dashed vertical line marking where the data
become incomplete (less than \code{level} of the delay distribution has arrived).
Holidays from an attached \code{\link[=temporal_effects]{temporal_effects()}} spec are marked with red dots.

\code{\link[=plot_epidemic_process]{plot_epidemic_process()}} draws the same curve without the incompleteness
line, next to its reporting twin \code{\link[=plot_reporting_process]{plot_reporting_process()}}.
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(denguedat)
dengue_now <- tbl_now(denguedat, onset_week, report_week, verbose = FALSE)
plot_observed_cases(dengue_now)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=autoplot.tbl_now]{autoplot.tbl_now()}}, \code{\link[=plot_epidemic_process]{plot_epidemic_process()}},
\code{\link[=plot_reporting_process]{plot_reporting_process()}}.
}
