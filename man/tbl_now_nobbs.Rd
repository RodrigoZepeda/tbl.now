% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/converters.R
\name{tbl_now_nobbs}
\alias{tbl_now_nobbs}
\alias{tbl_now_to_nobbs}
\title{Convert a \code{tbl_now} into the line list \pkg{NobBS} nowcasts from}
\usage{
tbl_now_to_nobbs(
  x,
  ...,
  event_col = "onset_date",
  report_col = "report_date",
  verbose = TRUE
)
}
\arguments{
\item{x}{A \code{tbl_now}.}

\item{...}{Unused, for extensibility.}

\item{event_col, report_col}{Names the two date columns should take in the
result. The defaults match the arguments of \code{\link[NobBS:NobBS]{NobBS::NobBS()}}.}

\item{verbose}{Print what the conversion did. The \code{units} line prints the
string \code{\link[NobBS:NobBS]{NobBS::NobBS()}} itself accepts (\code{"1 day"} or \code{"1 week"}), not the
object's own \code{"days"} / \code{"weeks"}, so it can be pasted straight into the
call.}
}
\value{
A \code{data.frame} with one row per case, ready for \code{\link[NobBS:NobBS]{NobBS::NobBS()}}.
The strata, covariates and temporal-effect columns ride along so a
per-stratum loop can split on them.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\code{\link[NobBS:NobBS]{NobBS::NobBS()}} counts \strong{rows}: it takes an individual-level line list with
one column for the event date and one for the report date, and treats each row
as a case. Handing it \code{count-incidence} data directly is therefore silently
wrong -- a table of 1,174 rows carrying 50,160 cases is nowcast as 1,174
cases. This converter expands counts to one row per case first, so the totals
NobBS sees are the totals in your data.

Trim \strong{before} converting when the series is long: the expansion is one row
per case, and \code{NobBS()}'s own \code{moving_window} only limits what it \emph{fits}, not
what it is handed.
}
\section{Units NobBS can model}{


\code{\link[NobBS:NobBS]{NobBS::NobBS()}} documents \code{units} as \code{"1 day"} or \code{"1 week"} and nothing
else, so this converter aborts on any other grid rather than hand back a line
list \pkg{NobBS} cannot use. That includes a \code{"numeric"} grid: its date
columns are integer indices, and coercing them with \code{as.Date()} would anchor
them at the 1970 epoch and return a plausible-looking line list of invented
dates. Aggregate to days or weeks first (see \code{\link[=align_weeks]{align_weeks()}}).
}

\section{Censored delays}{


A censoring indicator that is a property of the \strong{case} rather than of the
delay -- an administrative "this date is only an upper bound" mark, say --
puts a censored and an uncensored row in the same
\verb{(event_date, report_date)} cell. A reporting triangle has one slot per cell,
so the extra dimension has to go before the conversion. It is removed
automatically, with a warning either way:
\itemize{
\item \strong{count data}: the counts are summed over the flag, leaving case totals
unchanged;
\item \strong{line lists}: the column is dropped, leaving one row per case.
}

\code{\link[=tbl_now_to_epidist]{tbl_now_to_epidist()}} is the exception and keeps the flag: estimating a
delay distribution is the one job that can use it.
}

\examples{
\dontshow{if (requireNamespace("NobBS", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(denguedat)
nowobj <- tbl_now(denguedat,
  event_date = "onset_week", report_date = "report_week", verbose = FALSE
)
nb <- tbl_now_to_nobbs(nowobj, verbose = FALSE)
head(nb)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=tbl_now_to_surveillance]{tbl_now_to_surveillance()}}, \code{\link[=tbl_now_to_epinowcast]{tbl_now_to_epinowcast()}}
}
