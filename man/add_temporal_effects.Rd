% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_temporal_effects.R,
%   R/compute_temporal_effects.R
\name{add_temporal_effects}
\alias{add_temporal_effects}
\alias{add_temporal_effects.data.frame}
\alias{add_temporal_effects.tbl_now}
\alias{compute_temporal_effects}
\title{Attach calendar effects to a \code{tbl_now}, and turn them into columns}
\usage{
add_temporal_effects(x, t_effects = NULL, overwrite = FALSE, ...)

\method{add_temporal_effects}{data.frame}(
  x,
  t_effects = NULL,
  overwrite = FALSE,
  ...,
  date_col = NULL,
  numeric_col = NULL,
  name_prefix = paste0(".", date_col),
  weekend_days = c("Sat", "Sun"),
  units = "days"
)

\method{add_temporal_effects}{tbl_now}(
  x,
  t_effects = NULL,
  overwrite = FALSE,
  ...,
  date_type = "event_date",
  weekend_days = c("Sat", "Sun")
)

compute_temporal_effects(x, overwrite = FALSE)
}
\arguments{
\item{x}{A \code{tbl_now} object or a \code{data.frame}.}

\item{t_effects}{A \code{\link[=temporal_effects]{temporal_effects()}} object codifying the
temporal effects to be used.}

\item{overwrite}{Logical. When \code{TRUE}, columns that already exist are
overwritten. When \code{FALSE} (the default) an existing column of the same name is
an error, so an accidental second computation cannot silently replace your data.}

\item{...}{Additional arguments (unused)}

\item{date_col}{The column which contains the \verb{<Date>} values from which
effects will be calculated. This applies to all \code{temporal_effects} except
for \code{seasonal}.}

\item{numeric_col}{The column which contains the values from which
the seasonal effects will be calculated. This applies only to seasonal
effects. For date-related effects (such as month or day of the week)
use \code{date_col}.}

\item{name_prefix}{Character. Prefix for the names of the created columns.}

\item{weekend_days}{A character or numeric vector defining which days count as
the weekend. Defaults to Saturday and Sunday.
\itemize{
\item Character: day names or abbreviations, case-insensitive --
\code{c("Mon", "Tuesday", "wed", ...)}.
\item Numeric: integers 1-7 in \code{\link[lubridate:wday]{lubridate::wday()}} numbering with
\code{week_start = 1}, so \strong{1 = Monday} and 7 = Sunday.
}}

\item{units}{Character. The time units \code{date_col} is measured in: \code{"days"}
(the default), \code{"weeks"}, \code{"months"} or \code{"years"}. It only affects the
\strong{holiday} column, which on a grid coarser than days becomes the \emph{share} of
the period's days that are holidays rather than a 0/1 indicator. For a
\code{tbl_now}, \code{\link[=compute_temporal_effects]{compute_temporal_effects()}} reads this off the object.}

\item{date_type}{Either \code{event_date} (default) or \code{report_date}
to add temporal effects to those columns.}
}
\value{
\code{add_temporal_effects()} returns the object with the specification recorded.
For a \code{tbl_now} \strong{no columns are added}; for a plain \code{data.frame}, which has
nowhere to record a specification, the columns are computed immediately.

\code{compute_temporal_effects()} returns a \code{tbl_now} with one column per effect
appended. The specification is kept, so it still prints; the names of the
columns just created are available from
\link[=nowcast_data_getters]{get_temporal_effect_cols()}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#stable}{\figure{lifecycle-stable.svg}{options: alt='[Stable]'}}}{\strong{[Stable]}}

These are the second and third steps of using calendar structure in a nowcast.
First you write down which patterns you want with \code{\link[=temporal_effects]{temporal_effects()}}; then:
\itemize{
\item \code{add_temporal_effects()} \strong{records} that request on the object. Nothing is
computed and no columns appear -- the specification is stored lazily, so it
survives filtering and joining without going stale.
\item \code{compute_temporal_effects()} \strong{materialises} it, building one column per
effect from the object's dates.
}

The split matters because the columns depend on the data. If you computed them
first and then filtered, or changed the event-date column, the columns would
silently describe the wrong rows. Recording the request and computing it at the
end avoids that.

Call \code{add_temporal_effects()} more than once to accumulate several
specifications on the same object; \code{compute_temporal_effects()} builds all of
them.
}
\examples{
data(denguedat)
dengue <- tbl_now(denguedat,
  event_date = "onset_week",
  report_date = "report_week",
  strata = "gender",
  verbose = FALSE
)

# Step 1-2: say you want a week-of-year effect, and record it.
dengue <- dengue |>
  add_temporal_effects(t_effects = temporal_effects(week_of_year = TRUE))

# The request is stored, but no column has been built yet.
get_temporal_effects(dengue)
get_temporal_effect_cols(dengue)

# Step 3: materialise it.
computed <- compute_temporal_effects(dengue)
get_temporal_effect_cols(computed)
head(computed[[get_temporal_effect_cols(computed)[1]]])

# Specifications accumulate, so you can add a second pattern ...
both <- dengue |>
  add_temporal_effects(t_effects = temporal_effects(month_of_year = TRUE))
get_temporal_effect_cols(compute_temporal_effects(both))

# ... swap the whole specification for another ...
dengue |>
  replace_temporal_effects(t_effects = temporal_effects(seasons = 52)) |>
  get_temporal_effects()

# ... or forget it entirely.
dengue |>
  remove_temporal_effects() |>
  get_temporal_effects()

}
\seealso{
\code{\link[=temporal_effects]{temporal_effects()}} to build the specification, and for what each effect means;
\link[=add]{replace_temporal_effects()} and \link[=add]{remove_temporal_effects()} to swap
or drop it; \link[=nowcast_data_getters]{get_temporal_effects()} and
\link[=nowcast_data_getters]{get_temporal_effect_cols()} to read back the request and
the columns; \link{calendar_effect_plots} to see the patterns;
\code{\link[=add]{add()}} for the other attribute setters.
}
