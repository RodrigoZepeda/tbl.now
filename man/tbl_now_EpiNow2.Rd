% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/converters.R
\name{tbl_now_EpiNow2}
\alias{tbl_now_EpiNow2}
\alias{tbl_now_to_EpiNow2}
\alias{tbl_now_from_EpiNow2}
\title{Convert between \code{tbl_now} and \pkg{EpiNow2}}
\usage{
tbl_now_to_EpiNow2(
  x,
  ...,
  target = c("estimate_infections", "regional_epinow", "estimate_truncation",
    "estimate_secondary", "estimate_dist"),
  snapshots = NULL,
  secondary_type = c("confirmed", "total", "retracted", "unknown"),
  accumulate = "auto",
  complete = "auto",
  verbose = TRUE,
  quiet = FALSE
)

tbl_now_from_EpiNow2(data, ..., report_dates = NULL, verbose = TRUE)
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{...}{Forwarded to \code{\link[=as_tbl_now]{as_tbl_now()}} (\code{from}); unused (\code{to}).}

\item{target}{Which \pkg{EpiNow2} entry point the result is for. See above.}

\item{snapshots}{For \code{"estimate_truncation"}: how many snapshots to emit,
taken from the \strong{latest} report dates. \code{NULL} (default) uses 5, matching
\code{EpiNow2::example_truncated}. One snapshot per distinct report date is
usually far more than the model can fit.}

\item{secondary_type}{For \code{"estimate_secondary"}: which revision outcomes to
count in the \code{secondary} stream. One of \code{"confirmed"} (default), \code{"total"},
\code{"retracted"} or \code{"unknown"}. Pending cases are not on the revision axis,
and \code{"net"} can be negative, which \code{EpiNow2::estimate_secondary()} cannot
represent as a count stream.}

\item{accumulate}{How to handle non-daily data. \code{"auto"} (default) lays a
weekly series on \pkg{EpiNow2}'s daily grid with an \code{accumulate} column;
\code{FALSE} passes the rows through unchanged, which is almost always wrong (see
\emph{Non-daily data}). Ignored for \code{"estimate_dist"}, which works in censoring
windows rather than on a grid.}

\item{complete}{For the series targets: fill event periods that have no
reports at all with zeroes, out to the object's \code{\link[=get_now]{get_now()}}, via
\code{\link[=complete_zeroes]{complete_zeroes()}}. \code{"auto"} (the default) does this for \strong{line-list}
input only. A line list has no row for a period in which nothing was
reported, so a series built from one stops at the last period that \emph{has} a
report -- short of the \code{now}, which is the period the nowcast is about.
Count data is left exactly as supplied, because it can say "observed zero"
itself. \code{TRUE} / \code{FALSE} force either behaviour; \code{TRUE} on
\code{count-cumulative} input de-accumulates it first. Ignored for
\code{"estimate_dist"}, which works in censoring windows rather than on a grid.}

\item{verbose}{Logical. Print the choices that were made.}

\item{quiet}{Logical. A \emph{different} channel from \code{verbose}: \code{verbose}
controls the informational summary of what the conversion did, while \code{quiet}
suppresses the lossy-conversion warning. Set both to keep a conversion
entirely silent.}

\item{data}{A \link{tbl_now_epinow2_snapshots}, or a plain list of \code{date}/\code{confirm}
data frames (e.g. \code{EpiNow2::example_truncated}), in which case
\code{report_dates} is required.}

\item{report_dates}{For \code{from}: a \code{Date} vector, one per snapshot, saying when
each was taken. Read from the object's attribute when it has one.}
}
\value{
For \code{to}, a \code{data.frame} or a \link{tbl_now_epinow2_snapshots}, according to
\code{target}. For \code{from}, a \code{tbl_now} of \code{data_type = "count-incidence"}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

\pkg{EpiNow2} takes several different input shapes, one per entry point, so
\code{tbl_now_to_EpiNow2()} is told which one you want with \code{target} -- named after
the \pkg{EpiNow2} function the result is passed to, so it can be handed over
unchanged:

\describe{
\item{\code{"estimate_infections"}}{a \code{data.frame} of \code{date} / \code{confirm}, the
series as known at \code{\link[=get_now]{get_now()}}. Also what \code{\link[EpiNow2:epinow]{EpiNow2::epinow()}} takes.}
\item{\code{"regional_epinow"}}{the same, plus a \code{region} column built from the
object's strata.}
\item{\code{"estimate_truncation"}}{a \link{tbl_now_epinow2_snapshots} list -- one
\code{date}/\code{confirm} snapshot per report date, which is the one \pkg{EpiNow2}
model that uses the report dimension a \code{tbl_now} exists to carry.}
\item{\code{"estimate_secondary"}}{a \code{data.frame} of \code{date} / \code{primary} /
\code{secondary}, where \code{primary} counts reported arrivals by \code{report_date}
and \code{secondary} counts resolved revisions by \code{revision_date}, filtered by
\code{secondary_type}. This is a \strong{repurposing} of
\code{\link[EpiNow2:estimate_secondary]{EpiNow2::estimate_secondary()}}: the model was written for two
epidemiological streams linked by a delay (cases and deaths, say), and
here the two streams are one series and its own revisions, so the fitted
delay is report-to-revision. The converter warns about the repurposing
when it runs.}
\item{\code{"estimate_dist"}}{the interval-censored \code{pdate_lwr} / \code{pdate_upr} /
\code{sdate_lwr} / \code{sdate_upr} / \code{obs_date} frame that
\code{\link[EpiNow2:estimate_dist]{EpiNow2::estimate_dist()}} fits a \strong{delay distribution} to (new in
\pkg{EpiNow2} 1.9.0). Count data rides along as the \code{n} weight column.
\code{estimate_dist()} vendors likelihood functions from
\href{https://primarycensored.epinowcast.org/}{primarycensored}, and its
help asks that you cite \pkg{primarycensored} alongside \pkg{EpiNow2}
when using it (\code{citation("primarycensored")}).  Like
\code{\link[=tbl_now_to_epidist]{tbl_now_to_epidist()}}, this target returns one row per observed delay:
the revision axis is dropped and undeclared columns are pooled, because
neither reaches \code{estimate_dist()} and so neither can keep two rows
apart.}
}

\code{tbl_now_from_EpiNow2()} inverts the snapshot form: snapshot \emph{k} is the series
as known at report date \emph{k}, so differencing consecutive snapshots recovers
\code{count-incidence} exactly. There is deliberately \strong{no} inverse for the other
targets: a single series has no report dimension to recover, a secondary
stream is already aggregated, and a delay distribution is not case data.
}
\section{Non-daily data}{


\pkg{EpiNow2} models a \strong{daily} process. As of 1.9.0 there is no \code{timestep},
\code{interval} or \code{period} argument on any of its entry points, so a weekly series
passed as one row per week is read as one row per \strong{day} and the fit is
silently wrong on the time axis -- no error, just an epidemic seven times too
fast.

Its own answer is the \code{accumulate} column (see \code{\link[EpiNow2:fill_missing]{EpiNow2::fill_missing()}}): the
series is laid on a daily grid and the filler days are marked to be added to
the next real observation. \code{accumulate = "auto"} does this from
\code{\link[=get_event_units]{get_event_units()}} for case-count targets, and from the shared
\code{report_units} / \code{revision_units} grid for \code{estimate_secondary}. Units
coarser than a week, and the \code{"numeric"} grid, are refused outright rather
than approximated.
}

\section{What EpiNow2 will not take}{

\itemize{
\item \code{\link[EpiNow2:estimate_delay]{EpiNow2::estimate_delay()}} takes a bare vector of delays. Its own help now
points at \code{estimate_dist()} as "the recommended replacement", and it throws
away the censoring a \code{tbl_now} carries, so there is no target for it either.
If you want it anyway, it is \code{x$.delay}.
}
}

\examples{
\dontshow{if (requireNamespace("EpiNow2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(denguedat)
nowobj <- tbl_now(denguedat[1:2000, ],
  event_date = "onset_week", report_date = "report_week", verbose = FALSE
)
## A single daily series for estimate_infections() -- the weekly data is laid
# on EpiNow2's daily grid.
head(tbl_now_to_EpiNow2(nowobj, verbose = FALSE, quiet = TRUE))

## Snapshots for estimate_truncation(), which uses the report dimension.
snaps <- tbl_now_to_EpiNow2(nowobj,
  target = "estimate_truncation", verbose = FALSE, quiet = TRUE
)
snaps
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=tbl_now_to_epidist]{tbl_now_to_epidist()}}, which builds the same censoring windows as
\code{target = "estimate_dist"} -- the two are different front ends onto one
delay-distribution schema.
}
