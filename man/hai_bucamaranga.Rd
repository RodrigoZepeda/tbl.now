% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data-iaas_bucaramanga.R
\docType{data}
\name{hai_bucamaranga}
\alias{hai_bucamaranga}
\title{Healthcare-Associated Infections -- Bucaramanga, Colombia 2016-2023}
\format{
A \link[tibble:tibble]{tibble} with 1,423 rows and 13 variables:
\describe{
\item{id}{\code{integer}. The source's \code{Orden} autonumber. Documented as unique
but repeated for 100 records (see above), so it is \emph{not} a reliable key.}
\item{specimen_date}{\code{Date}. The event date -- when the sample was taken
from the patient. \code{NA} for 318 records.}
\item{received_date}{\code{Date}. When the laboratory received the sample. \code{NA}
for 1,194 records and absent after 2019-12-01.}
\item{report_date}{\code{Date}. The report date -- when the laboratory result was
issued. \code{NA} for 583 records.}
\item{specimen}{\code{factor}, 6 levels. Sample type: \code{"Whole blood"},
\code{"Urine"}, \code{"Secretions"}, \code{"Other sterile fluids"}, \code{"Sputum"},
\code{"Bronchoalveolar lavage"}. \code{NA} for clinically-confirmed cases.}
\item{test}{\code{factor}, 7 levels. Laboratory test performed, e.g.
\code{"Blood culture"}, \code{"Urine culture"}. \code{NA} for clinically-confirmed
cases.}
\item{microorganism}{\code{character}, 85 distinct values. The isolated organism,
in conventional binomial form, e.g. \code{"Klebsiella pneumoniae"}. \code{NA} for
clinically-confirmed cases. Where the source recorded only a genus the
value is \code{"<Genus> spp."}.}
\item{sex}{\code{factor}. \code{"Female"} or \code{"Male"}.}
\item{age_group}{\verb{ordered factor}, 11 levels from \code{"<1"} to \code{"70+"}.}
\item{case_type}{\code{factor}. \code{"Laboratory-confirmed"} (1,090) or
\code{"Clinically-confirmed"} (333).}
\item{final_condition}{\code{factor}. Patient status on discharge: \code{"Alive"} or
\code{"Dead"}. \code{NA} for 8 records.}
\item{icu_type}{\code{factor}. Intensive-care unit where the case occurred:
\code{"Adult"}, \code{"Paediatric"} or \code{"Neonatal"}.}
\item{institution}{\code{integer}, 1-10. Anonymised code of the reporting
health institution.}
}
}
\source{
Secretaria de Salud y Ambiente de Bucaramanga, via the Colombian
national open-data portal
\url{https://www.datos.gov.co/Salud-y-Protecci-n-Social/48-Infecciones-asociadas-a-la-atenci-n-en-salud-IA/w4zx-wbff/about_data}.
Retrieved 2026-08-17. Column names and categorical values translated from
Spanish; see \code{data-raw/hai_bucamaranga.R} for the mapping.
}
\usage{
hai_bucamaranga
}
\description{
A line list of healthcare-associated infections (IAAS, \emph{Infecciones Asociadas
a la Atencion en Salud}) notified in the municipality of Bucaramanga,
Santander, Colombia, between January 2016 and January 2023. Each row is one
notified infection: a specimen taken from a hospitalised patient, the
laboratory result, and the isolated microorganism.
}
\details{
In the nowcasting context the \strong{event date} is \code{specimen_date} (when the
sample was taken from the patient) and the \strong{report date} is \code{report_date}
(when the laboratory result was issued). The delay between the two is the
specimen-to-result turnaround that a nowcasting model would estimate and
correct for. A third date, \code{received_date}, records when the laboratory
received the sample and splits the delay into a transport and a processing
leg -- but see the warnings below before relying on it.

The column names and all categorical values have been translated from the
original Spanish. The dataset has been trimmed to the columns relevant to a
delay or nowcasting analysis; see \code{data-raw/hai_bucamaranga.R} in the
package sources for the full translation tables and the list of dropped
columns.
}
\section{Data quality -- read this first}{


This is a real, unpolished open-data extract, and it is included partly
\emph{because} it is messy: it is a realistic exercise for the delay diagnostics in
this package (\code{\link[=test_delay_drift]{test_delay_drift()}}, \code{\link[=test_delay_changepoint]{test_delay_changepoint()}},
\code{\link[=plot_delay_profiles]{plot_delay_profiles()}}). Nothing below has been silently repaired.

\describe{
\item{Missing dates}{The source uses \code{1900-01-01} as an undocumented
missing-date sentinel. It has been converted to \code{NA} here -- left in, it
produces delays of about -45,000 days. After conversion \code{specimen_date}
is present for 1,105 records (77.7\%), \code{report_date} for 840 (59.0\%) and
\code{received_date} for only 229 (16.1\%).}
\item{\code{received_date} is largely unusable}{Beyond being 84\% missing, it
stops entirely at 2019-12-01, so the three-date chain exists only for the
first third of the study period. Prefer the \code{specimen_date} ->
\code{report_date} pair.}
\item{Negative delays}{88 records (10.7\% of the otherwise-valid pairs) have
a \code{report_date} \emph{before} their \code{specimen_date}, by up to 331 days. These
are kept exactly as recorded. Filter them out before fitting anything.}
\item{Exact duplicates}{100 records are byte-identical duplicates of another
record, sharing the \code{id} that the source documents as a unique
autonumber. All 1,423 rows are shipped for fidelity; use
\code{\link[dplyr:distinct]{dplyr::distinct()}} to reduce to the 1,323 unique records.}
\item{Clinically-confirmed cases}{For the 333 cases confirmed on clinical
grounds rather than by laboratory, the source wrote the literal string
\code{"CONFIRMADO POR CLINICA"} into the \code{muestra}, \verb{nombre prueba} and
\code{microorganismo} fields. Those are \code{NA} here; the information is preserved
in \code{case_type}.}
\item{Sparsity}{Only 738 records (51.9\%) support a non-negative
\code{specimen_date} -> \code{report_date} delay, spread over 488 distinct event
dates -- roughly 1.5 cases per event date. That is thin for fitting a
nowcasting model, though ample for delay diagnostics. Aggregating to
weeks or months is usually necessary.}
}

The delay distribution is strongly bimodal: the median is 3 days but the 90th
percentile is 92 days, which makes this a useful test case for delay
diagnostics that assume a unimodal delay.
}

\examples{
data(hai_bucamaranga)

# The source ships 100 exact duplicates and a number of unusable rows.
# Reduce to unique records with a valid, non-negative reporting delay.
iaas_clean <- dplyr::distinct(hai_bucamaranga) |>
  dplyr::filter(
    !is.na(specimen_date), !is.na(report_date),
    report_date >= specimen_date
  )
nrow(iaas_clean)

# Roughly 1.5 cases per event date, so aggregate to weeks before building a
# tbl_now for anything model-shaped.
iaas_now <- tbl_now(
  iaas_clean,
  event_date  = "specimen_date",
  report_date = "report_date",
  verbose     = FALSE
)
iaas_now

# The delay is strongly bimodal: a 3-day median with a long secondary mode.
quantile(iaas_now$.delay, c(0.5, 0.75, 0.9, 0.99), na.rm = TRUE)
}
\seealso{
\link{covid_colombia} for a second Colombian surveillance dataset,
\code{\link[=test_delay_drift]{test_delay_drift()}} and \code{\link[=test_delay_changepoint]{test_delay_changepoint()}} for the delay
diagnostics this dataset is well suited to.
}
\keyword{datasets}
