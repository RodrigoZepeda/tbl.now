% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/confirmation.R
\name{censor_confirmation_delays_above}
\alias{censor_confirmation_delays_above}
\title{Mark long confirmation delays as censored}
\usage{
censor_confirmation_delays_above(data, max_delay, quiet = FALSE)
}
\arguments{
\item{data}{A \code{tbl_now} with a confirmation process.}

\item{max_delay}{Longest confirmation delay to keep, in the object's
confirmation units.}

\item{quiet}{Suppress the summary message.}
}
\value{
A \code{tbl_now}. Rows beyond \code{max_delay} have their
\code{confirmation_type} set to \code{"pending"} and their confirmation date set to
\code{NA}, because a resolution you refuse to believe is not a resolution.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The confirmation counterpart of \code{\link[=censor_delays_above]{censor_delays_above()}}. A case still waiting
for a laboratory result long after it was reported is, in practice, never
going to be resolved -- and treating its delay as a real observation drags
the estimated delay distribution to the right. This marks those cases as
\strong{pending} rather than letting an implausible delay stand.
}
\examples{
cases <- data.frame(
  onset = as.Date("2021-01-04") + 0:4,
  visit = as.Date("2021-01-05") + 0:4,
  result = as.Date("2021-01-05") + 0:4 + c(1, 2, 1, 90, 2),
  outcome = rep("confirmed", 5)
)
flu <- tbl_now(cases,
  event_date = onset, report_date = visit,
  confirmation_date = result, confirmation_type = outcome,
  data_type = "linelist", verbose = FALSE
)

# The 90-day resolution becomes "pending" again.
table(censor_confirmation_delays_above(flu, 30)[["outcome"]])

}
\seealso{
\code{\link[=censor_delays_above]{censor_delays_above()}} for the reporting delay,
\code{\link[=diagnose_confirmation_delay]{diagnose_confirmation_delay()}}.
}
