% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/batch_shape.R
\name{diagnose_batch_shape}
\alias{diagnose_batch_shape}
\title{Test whether one report date drew from unusually old event dates}
\usage{
diagnose_batch_shape(
  x,
  at,
  neighbours = 3L,
  guard = 1L,
  permute = c("items", "blocks"),
  n_permutations = 999L,
  axis = c("report", "confirmation"),
  seed = NULL
)
}
\arguments{
\item{x}{A \code{\link[=tbl_now]{tbl_now()}} object.}

\item{at}{The candidate report date (coercible to the class of the report
column), typically one flagged by \code{\link[=diagnose_batches]{diagnose_batches()}}.}

\item{neighbours}{Number of report dates on each side used as the reference
group.  Default \code{3}.}

\item{guard}{Number of report dates immediately either side of \code{at} to skip.
Default \code{1}.  Increase it to at least the longest plausible stall.}

\item{permute}{\code{"items"} (default; exact under log-linear intensity and
Poisson counts) or \code{"blocks"} (permutes whole report dates; valid under
overdispersion).}

\item{n_permutations}{Number of permutations. Default \code{999}.}

\item{axis}{Which time axis to scan for arrivals: \code{"report"} (default) or
\code{"confirmation"}. The question is the same either way -- did an unusual
number of records land on this date? -- so a laboratory clearing its
backlog is found exactly as a surveillance system clearing its inbox is.
\code{"confirmation"} needs a confirmation process (see \code{\link[=add_confirmation]{add_confirmation()}})
and ignores cases that are still \code{"pending"}, which have no confirmation
date to arrive on.}

\item{seed}{Optional RNG seed.}
}
\value{
A tibble, one row per stratum, with \code{stratum}, \code{n_at},
\code{n_reference}, \code{mean_delay_at}, \code{mean_delay_reference}, \code{statistic}
(standardised rank-sum) and \code{p_value} (one-sided: longer delays on \code{at}).
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

A complement to \code{\link[=diagnose_batches]{diagnose_batches()}}, which sees only report \emph{volumes}.  This test
asks whether the reports that arrived on a candidate date came from
systematically \emph{older} event dates -- the signature of a released backlog --
by comparing their delays with those of neighbouring report dates.  It is
\strong{model-free} and, under the conditions below, \strong{exactly distribution-free}.
}
\details{
The delays of the reports arriving on \code{at} are compared with the pooled delays
of the reports arriving on nearby dates, using a one-sided rank-sum (Wilcoxon)
statistic directed at \emph{longer} delays on \code{at}. The p-value comes from a
permutation, so no asymptotic approximation is used.

The test is model-free: as long as the epidemic curve is locally smooth,
neighbouring report dates share one common delay profile, so their delay labels
are exchangeable and the permutation test is (essentially) distribution-free --
it needs neither the delay distribution nor the epidemic curve. With Poisson
counts \code{permute = "items"} is exact; if the counts are overdispersed
(neighbouring report dates share event dates, so individual items are not
exchangeable) use \code{permute = "blocks"}, which permutes whole report dates.

The \code{guard} argument omits report dates immediately adjacent to \code{at} from the
comparison set: if a batch is present, its own deficit dates sit right beside
the spike and would contaminate the reference group. For \code{"count-cumulative"}
data only positive increments carry a meaningful delay; negative increments
(down-revisions) are dropped with a message.
}
\examples{
data(denguedat)

dengue_tbl <- tbl_now(
  denguedat,
  event_date  = onset_week,
  report_date = report_week,
  data_type   = "linelist",
  verbose     = FALSE
)

# Pick a report date to interrogate. A real workflow takes this from
# diagnose_batches(); here we simply name one.
diagnose_batch_shape(dengue_tbl, at = as.Date("1990-06-25"), n_permutations = 99)

# `n_permutations` sets the resolution of the p-value: 99 keeps the example
# fast, but use the default (999) for anything you intend to report.

}
\seealso{
\code{\link[=diagnose_batches]{diagnose_batches()}}, which finds the report dates worth passing to \code{at};
\code{\link[=simulate_batch]{simulate_batch()}} to plant a batch of known shape and check it is recovered;
\code{\link[=plot_delay_profiles]{plot_delay_profiles()}} to see the delay profile this tests.
}
