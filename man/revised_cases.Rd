% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_reported_cases.R
\name{revised_cases}
\alias{revised_cases}
\alias{get_latest_revised_cases}
\alias{get_initial_revised_cases}
\alias{get_nth_revised_cases}
\title{Cases at a chosen point in the revision process}
\usage{
get_latest_revised_cases(x, type = "total")

get_initial_revised_cases(x, type = "total")

get_nth_revised_cases(x, delay, type = "total")
}
\arguments{
\item{x}{A \code{tbl_now} with a revision process (see \code{\link[=add_revision_date]{add_revision_date()}}).}

\item{type}{Which cases to count. One of:
\describe{
\item{\code{"total"}}{(default) every case, whatever the outcome. On the
revision axis that means every case that has been settled at all.}
\item{\code{"confirmed"}, \code{"retracted"}, \code{"pending"}}{only the cases with that
outcome. \code{"pending"} is a reporting-axis question only -- a pending case
has no revision date -- and the revision getters refuse it.}
\item{\code{"unknown"}}{the cases whose \code{revision_type} is \code{NA}: settled, but
the data does not say which way.}
\item{\code{"net"}}{confirmed \strong{minus} retracted -- the running total as a
surveillance system publishes it, which can go \strong{down} when a case is
withdrawn. This is the quantity a \code{count-cumulative} stream actually
reports, and the one \pkg{diseasenowcasting}'s cumulative signed-change
likelihood is built for.}
\item{\code{"by_type"}}{one row per outcome instead of one number: the
outcome column joins the keys, so you get pending, confirmed and
retracted side by side.}
}
On an object with no revision process anything but \code{"total"} warns and
pools, because there is no outcome to filter on.}

\item{delay}{A single non-negative number (or \code{Inf}) giving the longest delay
\strong{from the event} to include, in the object's units. Only used by
\code{get_nth_revised_cases()}.}
}
\value{
A \code{count-cumulative} \code{tbl_now} with one row per event date (and
stratum, grouping column, and outcome when \code{type = "by_type"}), carrying the
event, report and revision dates of the selected arrival, the generated
numeric columns, and the count.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The same three questions as \link[=get_latest_first]{get_latest_reported_cases()},
asked of the \strong{third} date: not when the system heard about a case, but when
the laboratory settled it.
\itemize{
\item \code{get_initial_revised_cases()} -- the count as of the \strong{first} result to
come back for that event date.
\item \code{get_latest_revised_cases()} -- the count as of the \strong{most recent}
result: everything settled so far.
\item \code{get_nth_revised_cases()} -- the count settled \strong{within a given delay of
the event}.
}

A case that is still \code{"pending"} has no revision date, so it has not
arrived on this axis and none of these count it. That is the point: the gap
between \link[=get_latest_first]{get_latest_reported_cases()} and
\code{get_latest_revised_cases()} is the backlog the laboratory still owes you.
}
\section{Which date the count is indexed by}{


By the \strong{event date}, as every other \verb{get_*_cases()} function is. A case
confirmed three weeks after onset still belongs to the week it began. If you
want counts by revision date instead, group on \code{get_revision_date(x)}
yourself -- that is a different question (how busy was the laboratory) and
this package does not silently answer it.
}

\section{Which delay \code{get_nth_revised_cases()} counts}{


The delay \strong{from the event}, so that \code{get_nth_reported_cases(x, 7)} and
\code{get_nth_revised_cases(x, 7)} describe the same seven days and can be read
against each other. It is deliberately \emph{not} \code{.revision_delay}, which is
the laboratory's turnaround measured from the report. \code{\link[=diagnose_drift]{diagnose_drift()}} and
\link[=summary.tbl_now]{summary()} make the same choice for the same reason.
}

\section{Grouping is respected}{

As with the reporting-axis getters: the caller's grouping becomes a key and
comes back on the result. See \link[=get_latest_first]{get_latest_reported_cases()}.
}

\examples{
cases <- data.frame(
  onset = as.Date("2021-01-04") + c(0, 0, 1, 1, 2),
  visit = as.Date("2021-01-05") + c(0, 0, 1, 1, 2),
  result = as.Date("2021-01-06") + c(0, 0, 1, 1, 2),
  outcome = c("confirmed", "retracted", "confirmed", "confirmed", "retracted")
)
flu <- tbl_now(cases,
  event_date = onset, report_date = visit,
  revision_date = result, revision_type = outcome,
  data_type = "linelist", verbose = FALSE
)

# Three answers to "how many cases were there?".
get_latest_reported_cases(flu) # everything reported
get_latest_revised_cases(flu, type = "confirmed") # only the positives
get_latest_revised_cases(flu, type = "net") # positives minus withdrawals

# Every outcome side by side.
get_latest_revised_cases(flu, type = "by_type")

# And the same question asked earlier in the process: what had come back by
# the first result, and within two days of onset.
get_initial_revised_cases(flu)
get_nth_revised_cases(flu, delay = 2)

}
\seealso{
\link[=get_latest_first]{get_latest_reported_cases()} for the same counts on the
reporting process; \link[=add]{add_revision_date()} to attach a revision;
\link{revision_delay} for how long resolution takes;
\code{\link[=plot_revision_status]{plot_revision_status()}} to see confirmed, retracted and pending over time.
}
