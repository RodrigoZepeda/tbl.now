% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nowcast_engine.R
\name{engine}
\alias{engine}
\title{Specify a nowcasting model and its arguments}
\usage{
engine(
  method,
  ...,
  min_date = NULL,
  quantile_levels = nowcast_quantile_levels(),
  label = NULL
)
}
\arguments{
\item{method}{A single string naming the method, e.g. \code{"epinowcast"}. Built-in
names are matched case-insensitively. See \code{\link[=list_nowcast_methods]{list_nowcast_methods()}}.}

\item{...}{Further arguments for the modelling function, passed through
untouched. In \code{engine()} this is \emph{every} argument; in the \verb{engine_*()}
functions it is whatever their named arguments do not already cover.}

\item{min_date}{How much history to fit on. One of
\itemize{
\item \code{NULL} (default) -- the whole series;
\item a \strong{\code{Date}} -- keep event dates on or after it;
\item a \strong{single number} -- keep the last \emph{n} periods before the object's
\code{now}, counted in the object's event units.
}

The number is usually what you want in a \code{\link[=nowcast_backtest]{nowcast_backtest()}}: \code{now} moves
between fits, and a fixed calendar date would make the fitted window grow as
the backtest walks forward, so the last fit would be trained on more data
than the first. Trimming is per engine on purpose -- \code{baselinenowcast} and
\code{diseasenowcasting} take a long series in their stride, while \code{epinowcast}
scales with the number of reference dates and is best given a window.}

\item{quantile_levels}{Numeric vector of probabilities to report the nowcast
at. Defaults to \code{\link[=nowcast_quantile_levels]{nowcast_quantile_levels()}}.

It lives on the engine because for some backends it is a \strong{fit-time model
argument}, not a way of summarising afterwards. \pkg{NobBS} computes
exactly the quantiles it is handed in \code{specs$quantiles} and keeps no draws,
so a level it was never asked for cannot be recovered, and
\pkg{surveillance} reports a fixed set and warns rather than interpolating.
The draw-keeping backends -- \code{baselinenowcast}, \code{diseasenowcasting},
\code{epinowcast} and \code{EpiNow2} -- answer any level after the fact.}

\item{label}{Name for this engine in a \code{\link[=nowcast_backtest]{nowcast_backtest()}} and in the
ensemble weights derived from one. Defaults to the method name. Give one
when the same package appears twice with different settings, which is the
whole reason two \code{diseasenowcasting} models can be weighted separately.}
}
\value{
An object of class \code{c(method, "nowcast_engine")}.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

An \strong{engine} is one modelling package plus every argument that package
needs. It is what \code{\link[=run_nowcast]{run_nowcast()}} and \code{\link[=nowcast_backtest]{nowcast_backtest()}} take, and it is the
object \code{\link[=nowcast_fit]{nowcast_fit()}} and \code{\link[=nowcast_tidy]{nowcast_tidy()}} dispatch on.

\code{engine()} is the general constructor and works for any registered method,
including one you wrote yourself. The \verb{engine_*()} functions are its
package-specific counterparts: each \strong{names} the arguments of the modelling
function it drives, so the ones that matter are visible in the signature and
at \code{?engine_nobbs} rather than buried in a \code{...} that silently swallows a
typo. Anything a named argument does not cover still goes through \code{...}.
}
\examples{
engine("baselinenowcast", draws = 500)

# Built-in names are matched case-insensitively
engine("nobbs", max_D = 10)

# The same package twice, told apart by `label`
engine("diseasenowcasting", label = "default")

}
\seealso{
\code{\link[=run_nowcast]{run_nowcast()}}, \code{\link[=nowcast_backtest]{nowcast_backtest()}}, \code{\link[=list_nowcast_methods]{list_nowcast_methods()}}, and
\code{vignette("custom-nowcast-models")} for writing a backend of your own.
}
