% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/report_batches.R
\name{plot_report_batches}
\alias{plot_report_batches}
\title{Visualise batch reporting over the report-date axis}
\usage{
plot_report_batches(
  x,
  ...,
  signals = c("volume", "delay"),
  threshold = 3,
  backlog_delay = NULL,
  backlog_quantile = 0.9,
  baseline_window = NULL,
  gap_window = 3,
  min_reports = 1,
  by_strata = FALSE,
  strata = NULL,
  palette = .tbl_now_palette()
)
}
\arguments{
\item{x}{A \code{tbl_now} object.}

\item{...}{Unused.}

\item{signals}{Character vector of the signals to require (AND-ed). Any of
\code{"volume"}, \code{"delay"}, \code{"span"}, \code{"gap"}. Defaults to
\code{c("volume", "delay")}.}

\item{threshold}{Robust-z cutoff a signal must reach to fire (default \code{3}).}

\item{backlog_delay}{Delay at/above which a report counts as backlog (for the
\code{backlog} column). \code{NULL} (default) uses the \code{backlog_quantile} of the
observed delays.}

\item{backlog_quantile}{Delay quantile used to set \code{backlog_delay} when it is
\code{NULL} (default \code{0.9}).}

\item{baseline_window}{Rolling-baseline width in report periods. \code{NULL}
(default) uses roughly two months.}

\item{gap_window}{Number of preceding periods assessed by the \code{"gap"} signal
(default \code{3}).}

\item{min_reports}{Minimum reports for a period to be eligible (default \code{1}).}

\item{by_strata}{Logical (default \code{FALSE}). Detect batches separately per
stratum.}

\item{strata}{Character vector of columns to group on when \code{by_strata = TRUE}.
\code{NULL} (default) uses the object's \code{strata}.}

\item{palette}{A named colour palette (defaults to the package palette).}
}
\value{
A \pkg{ggplot2} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
}
\details{
Two stacked panels over the report date: the \strong{reporting volume} and the
\strong{mean reporting delay} of each report date, with the batches detected by
\code{\link[=detect_report_batches]{detect_report_batches()}} marked. A batch shows up as a spike in \emph{both} the
volume and the delay (a backlog of old cases cleared at once), which is what
separates it from an epidemic peak (a volume spike with a normal, short
delay).
}
\examples{
\dontshow{if (requireNamespace("ggplot2", quietly = TRUE)) withAutoprint(\{ # examplesIf}
data(denguedat)
dengue <- tbl_now(denguedat,
  event_date = "onset_week", report_date = "report_week", verbose = FALSE
)
plot_report_batches(dengue)
\dontshow{\}) # examplesIf}
}
\seealso{
\code{\link[=detect_report_batches]{detect_report_batches()}}
}
