% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/converters.R
\name{tbl_now_surveillance_list}
\alias{tbl_now_surveillance_list}
\alias{print.tbl_now_surveillance_list}
\title{One \pkg{surveillance} line list per stratum}
\usage{
\method{print}{tbl_now_surveillance_list}(x, ...)
}
\arguments{
\item{x}{A \code{tbl_now_surveillance_list}.}

\item{...}{Ignored.}
}
\value{
\code{print()} returns \code{x} invisibly.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

The object returned by
\code{tbl_now_to_surveillance(x, format = "linelist_list")}: one individual-level
line list per observed combination of the object's strata, together with the
metadata needed to rebuild a \code{tbl_now} from it.

It is a \strong{thin} class -- it is still a list of plain data frames, so
\code{lapply()}, \code{[[} and friends work as usual:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{pieces <- tbl_now_to_surveillance(x, format = "linelist_list")
lapply(pieces, function(piece) \{
  surveillance::nowcast(
    now = get_now(x), when = get_surveillance_when(x),
    data = piece, dEventCol = "dHospital", dReportCol = "dReport",
    control = list(dRange = get_surveillance_range(x))
  )
\})
}\if{html}{\out{</div>}}

The class exists for the same reason \link{tbl_now_triangle_list} does: printing
says plainly that these are strata rather than something else shaped like a
list of line lists, and it carries the \code{now}, the units and the original
date-column names, none of which survive in a bare \code{split()}.

\code{now} and the time grid are deliberately \strong{not} baked into each piece. The
grid must come from the whole object (\code{\link[=get_surveillance_range]{get_surveillance_range()}}), not from
the piece: every stratum has to be laid on the same axis, or a stratum whose
first case arrived late starts its own time on a different day.

\code{as_tbl_now()} binds the pieces back together and restores the original
date-column names, the strata and the covariates. Two things do \strong{not}
survive, because they are not in the line list to survive: count input comes
back as a \code{"linelist"} (one row per case, so the totals are unchanged but the
\code{case_count} column is gone), and materialised temporal-effect columns come
back as ordinary columns rather than as a spec.
}
\seealso{
\code{\link[=tbl_now_to_surveillance]{tbl_now_to_surveillance()}}, \code{\link[=as_tbl_now]{as_tbl_now()}}, \link{tbl_now_triangle_list}
}
