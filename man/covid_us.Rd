% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data.R
\docType{data}
\name{covid_us}
\alias{covid_us}
\title{covid_us: CDC COVID-19 Case Surveillance Public Use Data (2020-2021)}
\format{
A data frame with three variables:
\describe{
\item{cdc_case_earliest_dt}{\code{Date}. The event date -- the earlier of the
clinical date and the date received by CDC.}
\item{cdc_report_dt}{\code{Date}. The report date -- when the case was first
reported to CDC.}
\item{n}{\code{integer}. Number of cases with this (event date, report date)
pair.}
}
}
\source{
Centers for Disease Control and Prevention (CDC), COVID-19 Response.
\emph{COVID-19 Case Surveillance Public Use Data} (version date: June 21, 2024).
\url{https://data.cdc.gov/Case-Surveillance/COVID-19-Case-Surveillance-Public-Use-Data/vbim-akqf/about_data}.
COVID-19 case surveillance data are collected by jurisdictions and reported
voluntarily to CDC.
}
\usage{
data(covid_us)
}
\description{
A compact aggregation of the U.S. CDC's individual-level COVID-19 case
surveillance database, prepared to illustrate \strong{batch reporting}. Each row is
a unique (event date, report date) pair with the number of cases \code{n}.
}
\details{
In the nowcasting context the \strong{event date} is \code{cdc_case_earliest_dt} (the
earlier of the clinical/specimen date and the date the case was received by
CDC) and the \strong{report date} is \code{cdc_report_dt} (the date the case was first
reported to CDC). The delay between them is enormous and heavily right-skewed:
cases were reported to CDC not smoothly but in large backlog dumps -- a textbook
batch-reporting pattern that \code{\link[=diagnose_batches]{diagnose_batches()}} and \code{\link[=transport_discriminant]{transport_discriminant()}}
recover.

Cases are kept when both their event date and their report date fall between
2020-01-01 and 2021-12-31 -- a self-consistent "as of the end of 2021" snapshot,
so the epidemic and its reporting are seen over the same two years. The handful
of rows whose report date precedes their event date (data-entry errors) were
dropped. See \code{data-raw/covid_us.R} for the exact duckdb aggregation of the
14 GB source file.
}
\examples{
data(covid_us)
tn <- tbl_now(
  covid_us,
  event_date  = cdc_case_earliest_dt,
  report_date = cdc_report_dt,
  case_count  = n,
  data_type   = "count-incidence",
  verbose     = FALSE
)
tn
}
\seealso{
\code{\link[=tbl_now]{tbl_now()}} to declare the date columns; \link[=tbl_now_summary]{summary()} and
\code{\link[=diagnose]{diagnose()}} to inspect the result; the package's other datasets --
\link{denguedat}, \link{mpoxdat}, \link{flusight}, \link{covid_colombia}, \link{covid_us} and
\link{hai_bucaramanga}.
}
\keyword{datasets}
