# Reconstruct a missing date column from integer delay

Reconstruct a missing date column from integer delay

## Usage

``` r
.reconstruct_date_from_delay(
  data,
  known_col,
  delay_col,
  units,
  new_col_name,
  direction
)
```
